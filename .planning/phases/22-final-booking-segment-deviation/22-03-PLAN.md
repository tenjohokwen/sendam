---
phase: 22-final-booking-segment-deviation
plan: 03
type: tdd
wave: 3
depends_on: ["22-02"]
files_modified:
  - src/test/java/com/softropic/sendam/gateway/billing/service/FinalBookingServiceTest.java
autonomous: true

must_haves:
  truths:
    - "BOOK-01 test verifies debit(actualTotal) called and segment alert created with delta < 0"
    - "BOOK-02 test verifies debit(actualTotal) called and NO alert created when delta == 0"
    - "BOOK-03 test verifies debit(actualTotal) called and segment alert created with delta > 0 but within reservation"
    - "BOOK-04 test verifies debit(reservedTotal) called, SMS_EXTRA_DEBIT applyLedgerEntry called, segment alert created"
    - "BOOK-05 test verifies drain client, platform absorbs shortfall, clientFreezeService.freeze() called, segment alert with shortfall"
    - "BOOK-06 test verifies partial platform absorption, platformFreezeService.freeze() called, both SEGMENT and PLATFORM_FREEZE alerts created"
    - "SEGDEV tests verify per-recipient breakdown is populated in alerts"
  artifacts:
    - path: "src/test/java/com/softropic/sendam/gateway/billing/service/FinalBookingServiceTest.java"
      provides: "Unit tests for all 6 BOOK scenarios and SEGDEV requirements"
      min_lines: 200
  key_links:
    - from: "FinalBookingServiceTest BOOK-05"
      to: "clientFreezeService.freeze()"
      via: "verify(clientFreezeService).freeze(eq(CLIENT_ID), anyString())"
      pattern: "clientFreezeService.*freeze"
    - from: "FinalBookingServiceTest BOOK-06"
      to: "platformFreezeService.freeze()"
      via: "verify(platformFreezeService).freeze(anyString(), eq(unrecovered))"
      pattern: "platformFreezeService.*freeze"
---

<objective>
Write Mockito/JUnit 5 unit tests for FinalBookingService covering all 6 BOOK scenarios plus SEGDEV per-recipient breakdown verification. This is the TDD verification step — Plan 02 implementations must satisfy these tests.

Purpose: Proves that all BOOK-01 through BOOK-06 and SEGDEV-01 through SEGDEV-04 requirements are mechanically verified, not just visually reviewed. Tests provide regression protection for Phase 23+ changes that touch the billing finalization path.
Output: FinalBookingServiceTest.java with 8+ test methods, all passing.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/22-final-booking-segment-deviation/22-RESEARCH.md
@.planning/phases/22-final-booking-segment-deviation/22-02-SUMMARY.md

# Implementation being tested
@src/main/java/com/softropic/sendam/gateway/billing/service/FinalBookingService.java
@src/main/java/com/softropic/sendam/gateway/billing/service/SegmentDeviationService.java

# Repos and entities needed for mocking
@src/main/java/com/softropic/sendam/gateway/billing/repo/ClientCreditBalance.java
@src/main/java/com/softropic/sendam/gateway/billing/repo/ClientCreditBalanceRepository.java
@src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditBalance.java
@src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditBalanceRepository.java
@src/main/java/com/softropic/sendam/gateway/sms/repo/SendRequest.java
@src/main/java/com/softropic/sendam/gateway/sms/repo/SendRequestRecipient.java
@src/main/java/com/softropic/sendam/gateway/sms/repo/SendRequestRepository.java
@src/main/java/com/softropic/sendam/gateway/sms/repo/SendRequestRecipientRepository.java

# Existing test for pattern reference
@src/test/java/com/softropic/sendam/gateway/billing/service/SmsFinalisedBillingListenerTest.java
</context>

<feature>
  <name>FinalBookingService — all 6 BOOK scenarios + SEGDEV</name>
  <files>
    src/test/java/com/softropic/sendam/gateway/billing/service/FinalBookingServiceTest.java
  </files>
  <behavior>
Test class structure:
- @ExtendWith(MockitoExtension.class)
- @Mock for all 9 dependencies: CreditReservationService, CreditService, PlatformCreditService, ClientFreezeService, PlatformFreezeService, SegmentDeviationService, ClientCreditBalanceRepository, PlatformCreditBalanceRepository, SendRequestRepository, SendRequestRecipientRepository
- @InjectMocks FinalBookingService

Constants:
- CLIENT_ID = 100L
- SEND_REQUEST_ID = "req-abc"
- RESERVATION_ID = 42L
- SEND_REQUEST_PK = 77L  (the numeric DB id of the SendRequest row)
- Expected segments per recipient: 2 (rawExpectedCredits = 4 for 2 recipients)
- Reserved credits: 6 (= (2+1) * 2 = buffer formula)
- Recipients: 2, each with expectedSegments=2

Helper: build a SendRequest stub with rawExpectedCredits=4, reservedCredits=6, reservationId=42L, clientId=100L, id=77L.
Helper: build 2 SendRequestRecipient stubs with expectedSegments=2 each.

Setup beforeEach: stub sendRequestRepository.findByClientIdAndSendRequestId(CLIENT_ID, SEND_REQUEST_ID) → Optional.of(sendRequest).
Setup: stub recipientRepository.findBySendRequestIdFk(SEND_REQUEST_PK) → List.of(recipient1, recipient2).

BOOK-01 (actualTotal=3, expectedTotal=4, reservedTotal=6 → delta=-1, within reservation):
- Stub: no clientLock needed (actualTotal <= reservedTotal path)
- Call: book(CLIENT_ID, SEND_REQUEST_ID, 3L, RESERVATION_ID)
- Verify: creditReservationService.debit(CLIENT_ID, RESERVATION_ID, 3L)
- Verify: segmentDeviationService.createAlert(argThat(d -> d.delta() == -1 && d.financialAction().equals("REFUNDED") && d.alertType() == DeviationAlertType.SEGMENT))
- Verify: creditService.applyLedgerEntry — never called
- Verify: clientFreezeService.freeze — never called

BOOK-02 (actualTotal=4, expectedTotal=4 → delta=0, no alert):
- Call: book(CLIENT_ID, SEND_REQUEST_ID, 4L, RESERVATION_ID)
- Verify: creditReservationService.debit(CLIENT_ID, RESERVATION_ID, 4L)
- Verify: segmentDeviationService.createAlert — never called
- Verify: clientFreezeService.freeze — never called

BOOK-03 (actualTotal=5, expectedTotal=4, reservedTotal=6 → delta=+1, within reservation):
- Call: book(CLIENT_ID, SEND_REQUEST_ID, 5L, RESERVATION_ID)
- Verify: creditReservationService.debit(CLIENT_ID, RESERVATION_ID, 5L)
- Verify: segmentDeviationService.createAlert(argThat(d -> d.delta() == 1 && d.financialAction().equals("REFUNDED") && d.alertType() == DeviationAlertType.SEGMENT))

BOOK-04 (actualTotal=8, reservedTotal=6, extra=2, clientAvailable=10 → client can cover):
- Stub: clientCreditBalanceRepository.findByClientIdForUpdate(CLIENT_ID) → Optional.of(clientLock with balance=10)
- Call: book(CLIENT_ID, SEND_REQUEST_ID, 8L, RESERVATION_ID)
- Verify: creditReservationService.debit(CLIENT_ID, RESERVATION_ID, 6L)  // full reservation
- Verify: creditService.applyLedgerEntry(CLIENT_ID, LedgerEntryType.SMS_EXTRA_DEBIT, -2L, anyString())
- Verify: segmentDeviationService.createAlert(argThat(d -> d.delta() == 4 && d.financialAction().equals("EXTRA_DEBITED") && d.shortfallAmount() == null))
- Verify: clientFreezeService.freeze — never called

BOOK-05 (actualTotal=8, reservedTotal=6, extra=2, clientAvailable=1 → shortfall=1, platform has 5):
- Stub: clientCreditBalanceRepository.findByClientIdForUpdate(CLIENT_ID) → Optional.of(clientLock balance=1)
- Stub: platformCreditBalanceRepository.findForUpdate() → Optional.of(platformLock balance=5)
- Call: book(CLIENT_ID, SEND_REQUEST_ID, 8L, RESERVATION_ID)
- Verify: creditReservationService.debit(CLIENT_ID, RESERVATION_ID, 6L)
- Verify: creditService.applyLedgerEntry(CLIENT_ID, LedgerEntryType.SMS_EXTRA_DEBIT, -1L, anyString())  // drain client (1 available)
- Verify: platformCreditService.applyLedgerEntry(PlatformLedgerEntryType.SHORTFALL_ABSORPTION, -1L, anyString())  // shortfall=1
- Verify: clientFreezeService.freeze(eq(CLIENT_ID), anyString())
- Verify: platformFreezeService.freeze — never called
- Verify: segmentDeviationService.createAlert(argThat(d -> d.shortfallAmount() == 1L && d.clientFrozen() && !d.platformFrozen() && d.financialAction().equals("SHORTFALL_ABSORBED")))

BOOK-06 (actualTotal=8, reservedTotal=6, extra=2, clientAvailable=0, platformAvailable=1, shortfall=2, unrecovered=1):
- Stub: clientCreditBalanceRepository.findByClientIdForUpdate(CLIENT_ID) → Optional.of(clientLock balance=0)
- Stub: platformCreditBalanceRepository.findForUpdate() → Optional.of(platformLock balance=1)
- Call: book(CLIENT_ID, SEND_REQUEST_ID, 8L, RESERVATION_ID)
- Verify: creditReservationService.debit(CLIENT_ID, RESERVATION_ID, 6L)
- Verify: creditService.applyLedgerEntry — never called (clientAvailable=0, nothing to drain)
- Verify: platformCreditService.applyLedgerEntry(PlatformLedgerEntryType.SHORTFALL_ABSORPTION, -1L, anyString())  // absorb only what's available
- Verify: clientFreezeService.freeze(eq(CLIENT_ID), anyString())
- Verify: platformFreezeService.freeze(anyString(), eq(1L))  // unrecovered=1
- Verify: segmentDeviationService.createAlert called twice — once for SEGMENT alert, once for PLATFORM_FREEZE alert
  Use: verify(segmentDeviationService, times(2)).createAlert(any())

SEGDEV per-recipient breakdown (use BOOK-01 scenario):
- recipients: stub getSegmentsConsumed() → 1 on both recipients (total=2, expected=4, delta=-2)
- Call: book(CLIENT_ID, SEND_REQUEST_ID, 2L, RESERVATION_ID)
- Capture the SegmentDeviationAlertData via ArgumentCaptor
- Assert: capturedData.perRecipientBreakdown().size() == 2
- Assert: each entry has expectedSegments=2, actualSegments=1, delta=-1

Zero segments (all-failed path — no alert, release instead):
- Call: book(CLIENT_ID, SEND_REQUEST_ID, 0L, RESERVATION_ID)
- Verify: creditReservationService.release(CLIENT_ID, RESERVATION_ID)
- Verify: creditReservationService.debit — never called
- Verify: segmentDeviationService.createAlert — never called
  </behavior>
  <implementation>
Follow the RED-GREEN-REFACTOR cycle:

RED: Write all test methods with stubs, run mvn test — tests compile but FinalBookingService scenarios fail as expected (if Plan 02 has any gaps, this reveals them).

GREEN: Fix any remaining gaps in FinalBookingService found during RED phase. Run mvn test until all pass.

REFACTOR: Extract repeated stub setup to @BeforeEach helper methods. Keep each test method under 20 lines of assertions.

Test naming convention (match project pattern from SmsFinalisedBillingListenerTest):
- book_book01_underExpected_refundsBuffer_createsSegmentAlert
- book_book02_exactExpected_noAlert
- book_book03_overExpectedWithinBuffer_createsSegmentAlert
- book_book04_overReserved_clientCovers_extraDebit_createsAlert
- book_book05_overReserved_clientShortfall_platformAbsorbs_clientFrozen_createsAlert
- book_book06_overReserved_platformAlsoShortfall_bothFrozen_twoAlerts
- book_zeroSegments_releasesReservation
- book_perRecipientBreakdown_populatedCorrectly

Important: When mocking SendRequest.getId() for sendRequestPk — SendRequest extends AbstractAuditingEntity which extends BaseEntity. Check BaseEntity for the getId() method to confirm it returns Long. Use `when(sendRequest.getId()).thenReturn(SEND_REQUEST_PK)` — confirm the mock approach works (either spy on a built object or use Mockito.mock(SendRequest.class)).
  </implementation>
</feature>

<verification>
After implementing:
- mvn test -q exits 0
- All 8 named test methods exist and pass
- Total test count is 202+ (194 existing + 8 new)
- No test uses @SpringBootTest — pure Mockito unit tests only
- grep -r "FinalBookingServiceTest" src/test/ finds the file
</verification>

<success_criteria>
All 8 FinalBookingServiceTest methods pass. Every BOOK scenario is mechanically verified by at least one test. SEGDEV per-recipient breakdown is verified by the capturedData test. Total test suite passes cleanly (202+ tests).
</success_criteria>

<output>
After completion, create `.planning/phases/22-final-booking-segment-deviation/22-03-SUMMARY.md` following the template at @~/.claude/get-shit-done/templates/summary.md
</output>
