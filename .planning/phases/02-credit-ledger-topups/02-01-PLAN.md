---
phase: 02-credit-ledger-topups
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V3__credit_ledger.sql
  - src/main/java/com/softropic/sendam/client/repo/ClientCreditBalance.java
  - src/main/java/com/softropic/sendam/client/repo/ClientCreditBalanceRepository.java
  - src/main/java/com/softropic/sendam/client/repo/CreditLedgerEntry.java
  - src/main/java/com/softropic/sendam/client/repo/CreditLedgerRepository.java
  - src/main/java/com/softropic/sendam/client/contract/LedgerEntryType.java
  - src/main/java/com/softropic/sendam/client/contract/BalanceResponse.java
  - src/main/java/com/softropic/sendam/client/contract/LedgerEntryDto.java
  - src/main/java/com/softropic/sendam/client/contract/LedgerHistoryResponse.java
  - src/main/java/com/softropic/sendam/client/service/CreditService.java
  - src/main/java/com/softropic/sendam/client/api/CreditResource.java
  - src/main/java/com/softropic/sendam/client/service/ClientService.java
autonomous: true

must_haves:
  truths:
    - "Authenticated client calling GET /v1/credits/balance receives a 200 with available_balance, unit, currency, last_updated_at"
    - "Authenticated client calling GET /v1/credits/ledger receives a 200 with a paginated entries array sorted newest-first"
    - "A newly created client always has a client_credit_balance row with balance=0 — no ResourceNotFoundException on first balance query"
    - "balance_after on each ledger entry matches the running balance at the time of write — not re-derived on read"
    - "Balance query completes in O(1) via direct SELECT on client_credit_balance — no SUM over ledger rows"
  artifacts:
    - path: "src/main/resources/db/migration/V3__credit_ledger.sql"
      provides: "DDL for client_credit_balance and credit_ledger_entry tables with indexes"
      contains: "CREATE TABLE main.client_credit_balance"
    - path: "src/main/java/com/softropic/sendam/client/repo/ClientCreditBalance.java"
      provides: "JPA entity for balance lock row — one row per client"
      exports: ["ClientCreditBalance"]
    - path: "src/main/java/com/softropic/sendam/client/repo/ClientCreditBalanceRepository.java"
      provides: "findByClientId (no lock) + findByClientIdForUpdate (PESSIMISTIC_WRITE)"
      exports: ["ClientCreditBalanceRepository"]
    - path: "src/main/java/com/softropic/sendam/client/repo/CreditLedgerEntry.java"
      provides: "JPA entity for append-only ledger entries"
      exports: ["CreditLedgerEntry"]
    - path: "src/main/java/com/softropic/sendam/client/repo/CreditLedgerRepository.java"
      provides: "findByClientIdOrderByCreatedDateDesc(clientId, Pageable)"
      exports: ["CreditLedgerRepository"]
    - path: "src/main/java/com/softropic/sendam/client/contract/LedgerEntryType.java"
      provides: "Enum: TOPUP_PENDING, TOPUP_APPROVED, SMS_RESERVATION, SMS_DEBIT, SMS_REFUND"
      exports: ["LedgerEntryType"]
    - path: "src/main/java/com/softropic/sendam/client/service/CreditService.java"
      provides: "getBalance, getLedgerHistory, applyLedgerEntry (used by Plan 02 and 03)"
      exports: ["CreditService"]
    - path: "src/main/java/com/softropic/sendam/client/api/CreditResource.java"
      provides: "GET /v1/credits/balance, GET /v1/credits/ledger endpoints"
      exports: ["CreditResource"]
  key_links:
    - from: "src/main/java/com/softropic/sendam/client/api/CreditResource.java"
      to: "src/main/java/com/softropic/sendam/client/service/CreditService.java"
      via: "constructor injection, @RequiredArgsConstructor"
      pattern: "CreditService.*getBalance|CreditService.*getLedgerHistory"
    - from: "src/main/java/com/softropic/sendam/client/service/CreditService.java"
      to: "src/main/java/com/softropic/sendam/client/repo/ClientCreditBalanceRepository.java"
      via: "findByClientId (non-locking read)"
      pattern: "balanceRepository\\.findByClientId"
    - from: "src/main/java/com/softropic/sendam/client/service/ClientService.java"
      to: "src/main/java/com/softropic/sendam/client/repo/ClientCreditBalanceRepository.java"
      via: "save() after ClientEntity save in createClient()"
      pattern: "clientCreditBalanceRepository\\.save"
---

<objective>
Implement the ledger foundation: the two core tables (client_credit_balance and credit_ledger_entry), their JPA entities and repositories, the CreditService with balance query and ledger history methods, and the two client-facing endpoints GET /v1/credits/balance and GET /v1/credits/ledger.

Purpose: Every subsequent Phase 2 and Phase 3 operation — top-up approval, credit reservation, SMS billing — depends on this plan's entities and CreditService.applyLedgerEntry() method existing. This plan must be complete before Plans 02 and 03 can execute.
Output: Two Flyway-managed tables, four JPA artifacts, CreditService with applyLedgerEntry(), two working REST endpoints, and ClientService extended to insert the balance lock row when a client is created.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-credit-ledger-topups/02-RESEARCH.md
@ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/01-client-api-key-auth/01-01-SUMMARY.md
@requirements/client-facing_API_contract_v8.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway migration V3 + JPA entities + repositories</name>
  <files>
    src/main/resources/db/migration/V3__credit_ledger.sql
    src/main/java/com/softropic/sendam/client/repo/ClientCreditBalance.java
    src/main/java/com/softropic/sendam/client/repo/ClientCreditBalanceRepository.java
    src/main/java/com/softropic/sendam/client/repo/CreditLedgerEntry.java
    src/main/java/com/softropic/sendam/client/repo/CreditLedgerRepository.java
    src/main/java/com/softropic/sendam/client/contract/LedgerEntryType.java
  </files>
  <action>
Create V3__credit_ledger.sql with the exact SQL below (schema = main, same as V2):

```sql
-- Lock row: one row per client, holds current balance for SELECT FOR UPDATE
CREATE TABLE main.client_credit_balance (
    id          BIGINT PRIMARY KEY,
    client_id   BIGINT NOT NULL UNIQUE REFERENCES main.client_account(id),
    balance     BIGINT NOT NULL DEFAULT 0,
    created_by           VARCHAR(50),
    created_date         TIMESTAMP,
    last_modified_by     VARCHAR(50),
    last_modified_date   TIMESTAMP,
    request_id           VARCHAR(100),
    session_id           TEXT
);
CREATE INDEX idx_client_credit_balance_client ON main.client_credit_balance(client_id);

-- Ledger: append-only, one entry per credit event
CREATE TABLE main.credit_ledger_entry (
    id            BIGINT PRIMARY KEY,
    client_id     BIGINT NOT NULL REFERENCES main.client_account(id),
    entry_type    VARCHAR(30) NOT NULL,
    amount        BIGINT NOT NULL,
    balance_after BIGINT NOT NULL,
    reference     VARCHAR(200),
    status        VARCHAR(20) NOT NULL DEFAULT 'ACTIVE',
    created_by           VARCHAR(50),
    created_date         TIMESTAMP,
    last_modified_by     VARCHAR(50),
    last_modified_date   TIMESTAMP,
    request_id           VARCHAR(100),
    session_id           TEXT
);
CREATE INDEX idx_ledger_client_date ON main.credit_ledger_entry(client_id, created_date DESC);
```

Create ClientCreditBalance.java in com.softropic.sendam.client.repo:
- @Entity, @Table(name = "client_credit_balance", schema = "main")
- Extends AbstractAuditingEntity (provides all audit columns + @Tsid id via BaseEntity)
- @SuperBuilder, @NoArgsConstructor, @AllArgsConstructor, @Getter, @Setter on class
- Fields: Long clientId (@Column(name = "client_id", nullable = false, unique = true)), long balance (@Column nullable = false) — use primitive long, never Long
- No @Version field — not needed; SELECT FOR UPDATE handles concurrency

Create ClientCreditBalanceRepository.java:
- extends JpaRepository<ClientCreditBalance, Long>
- Optional<ClientCreditBalance> findByClientId(Long clientId) — non-locking read for balance query endpoint
- @Lock(LockModeType.PESSIMISTIC_WRITE) + @Query("SELECT b FROM ClientCreditBalance b WHERE b.clientId = :clientId") + @QueryHints({@QueryHint(name = "jakarta.persistence.lock.timeout", value = "2000")}) Optional<ClientCreditBalance> findByClientIdForUpdate(@Param("clientId") Long clientId) — used by CreditService.applyLedgerEntry() and Plan 03 CreditReservationService

Create CreditLedgerEntry.java in com.softropic.sendam.client.repo:
- @Entity, @Table(name = "credit_ledger_entry", schema = "main")
- Extends AbstractAuditingEntity
- @SuperBuilder, @NoArgsConstructor, @AllArgsConstructor, @Getter, @Setter
- Fields:
  - Long clientId (@Column(name = "client_id", nullable = false))
  - @Enumerated(EnumType.STRING) LedgerEntryType entryType (@Column(name = "entry_type", nullable = false, length = 30))
  - long amount (@Column nullable = false) — signed: positive = credit, negative = debit
  - long balanceAfter (@Column(name = "balance_after", nullable = false))
  - String reference (@Column(length = 200)) — nullable, holds topup_id, sendRequestId, etc.
  - @Enumerated(EnumType.STRING) EntityStatus status (@Column nullable = false) — set to EntityStatus.ACTIVE on creation

Create CreditLedgerRepository.java:
- extends JpaRepository<CreditLedgerEntry, Long>
- Page<CreditLedgerEntry> findByClientIdOrderByCreatedDateDesc(Long clientId, Pageable pageable)

Create LedgerEntryType.java in com.softropic.sendam.client.contract:
- public enum LedgerEntryType { TOPUP_PENDING, TOPUP_APPROVED, SMS_RESERVATION, SMS_DEBIT, SMS_REFUND }
- No Spring annotations — this is a contract type
  </action>
  <verify>
Run: mvn compile -pl . -q
Expected: BUILD SUCCESS with no compilation errors. Hibernate schema validation (if ddl-auto=validate) will fail at startup until the Flyway migration runs — that is expected; compilation is the check here.
  </verify>
  <done>
V3__credit_ledger.sql exists with both CREATE TABLE statements. ClientCreditBalance, CreditLedgerEntry, ClientCreditBalanceRepository, CreditLedgerRepository, and LedgerEntryType all compile without errors. ClientCreditBalanceRepository has both findByClientId and findByClientIdForUpdate methods with correct @Lock annotation.
  </done>
</task>

<task type="auto">
  <name>Task 2: CreditService, contract types, CreditResource, ClientService extension</name>
  <files>
    src/main/java/com/softropic/sendam/client/contract/BalanceResponse.java
    src/main/java/com/softropic/sendam/client/contract/LedgerEntryDto.java
    src/main/java/com/softropic/sendam/client/contract/LedgerHistoryResponse.java
    src/main/java/com/softropic/sendam/client/service/CreditService.java
    src/main/java/com/softropic/sendam/client/api/CreditResource.java
    src/main/java/com/softropic/sendam/client/service/ClientService.java
  </files>
  <action>
Create BalanceResponse.java in com.softropic.sendam.client.contract:
- public record BalanceResponse(@JsonProperty("available_balance") long availableBalance, String unit, String currency, @JsonProperty("last_updated_at") Instant lastUpdatedAt) {}
- No Spring annotations

Create LedgerEntryDto.java in com.softropic.sendam.client.contract:
- public record LedgerEntryDto(Instant timestamp, LedgerEntryType type, long amount, @JsonProperty("balance_after") long balanceAfter, String reference) {}

Create LedgerHistoryResponse.java in com.softropic.sendam.client.contract:
- public record LedgerHistoryResponse(List<LedgerEntryDto> entries, int page, int size, long totalElements) {}

Create CreditService.java in com.softropic.sendam.client.service:
- @Service, @Transactional at class level, @Slf4j, @RequiredArgsConstructor
- Inject: ClientCreditBalanceRepository balanceRepository, CreditLedgerRepository ledgerRepository
- Method: @Transactional(readOnly = true) BalanceResponse getBalance(Long clientId)
  - calls balanceRepository.findByClientId(clientId).orElseThrow(() -> new ResourceNotFoundException("No credit balance row for client: " + clientId))
  - returns new BalanceResponse(row.getBalance(), "segments", "SMS_SEGMENT", row.getLastModifiedDate())
- Method: @Transactional(readOnly = true) LedgerHistoryResponse getLedgerHistory(Long clientId, int page, int size)
  - clamp size to max 200: int effectiveSize = Math.min(size, 200)
  - Pageable pageable = PageRequest.of(page, effectiveSize)
  - Page<CreditLedgerEntry> pageResult = ledgerRepository.findByClientIdOrderByCreatedDateDesc(clientId, pageable)
  - maps entries: entry -> new LedgerEntryDto(entry.getCreatedDate(), entry.getEntryType(), entry.getAmount(), entry.getBalanceAfter(), entry.getReference())
  - returns new LedgerHistoryResponse(dtos, pageResult.getNumber(), pageResult.getSize(), pageResult.getTotalElements())
- Method: void applyLedgerEntry(Long clientId, LedgerEntryType type, long amount, String reference)
  - This is the core write method called by TopupService (Plan 02) and CreditReservationService (Plan 03)
  - Acquires lock: ClientCreditBalance lockRow = balanceRepository.findByClientIdForUpdate(clientId).orElseThrow(() -> new ResourceNotFoundException("No credit balance row for client: " + clientId))
  - long currentBalance = lockRow.getBalance(); long newBalance = currentBalance + amount;
  - If newBalance < 0: throw new InsufficientBalanceException("Insufficient balance: available=" + currentBalance + ", requested=" + Math.abs(amount)) — create InsufficientBalanceException in com.softropic.sendam.client.contract.exception, extends ApplicationException, carries clientId, currentBalance, requestedAmount fields
  - Writes ledger entry: CreditLedgerEntry entry = CreditLedgerEntry.builder().clientId(clientId).entryType(type).amount(amount).balanceAfter(newBalance).reference(reference).status(EntityStatus.ACTIVE).build(); ledgerRepository.save(entry);
  - Updates lock row: lockRow.setBalance(newBalance); balanceRepository.save(lockRow);
  - NOTE: TOPUP_PENDING entries (amount=0) pass through without modifying balance — that is intentional

Create CreditResource.java in com.softropic.sendam.client.api:
- @RestController, @RequestMapping("/v1/credits"), @RequiredArgsConstructor, @Slf4j
- Inject: CreditService creditService
- GET /v1/credits/balance:
  - Method: ResponseEntity<BalanceResponse> getBalance()
  - Extract clientId from SecurityContextHolder: Long clientId = (Long) SecurityContextHolder.getContext().getAuthentication().getPrincipal();
  - return ResponseEntity.ok(creditService.getBalance(clientId));
- GET /v1/credits/ledger:
  - Method: ResponseEntity<LedgerHistoryResponse> getLedgerHistory(@RequestParam(defaultValue = "0") int page, @RequestParam(defaultValue = "50") int size)
  - Extract clientId same way
  - return ResponseEntity.ok(creditService.getLedgerHistory(clientId, page, size));
- Both endpoints are on the /v1/** security chain (@Order(1) = ClientSecurityConfiguration) — API key authentication applies automatically. No additional @PreAuthorize needed.

Modify ClientService.java (existing file in com.softropic.sendam.client.service):
- Inject ClientCreditBalanceRepository clientCreditBalanceRepository (add to @RequiredArgsConstructor constructor — check if the class uses @RequiredArgsConstructor or manual constructor; add the field accordingly)
- In createClient() method, after clientRepository.save(clientEntity), add:
  ClientCreditBalance balanceRow = ClientCreditBalance.builder()
      .clientId(clientEntity.getId())
      .balance(0L)
      .status(EntityStatus.ACTIVE)
      .build();
  clientCreditBalanceRepository.save(balanceRow);
- This ensures every new client has a balance row. Do NOT add this in a separate @PostPersist listener — it must be in the same transaction as createClient().

Create InsufficientBalanceException.java in com.softropic.sendam.client.contract.exception:
- extends ApplicationException (from com.softropic.sendam.common.exception)
- Constructor: InsufficientBalanceException(String message) — calls super with appropriate ErrorCode; use a new INSUFFICIENT_CLIENT_BALANCE error code constant or reuse existing pattern. Check what ErrorCode enum/interface exists in the codebase and follow that pattern exactly.
- Add @ExceptionHandler to ApiAdvice (com.softropic.sendam.security.api.ApiAdvice) returning 400 Bad Request with error_code "INSUFFICIENT_CLIENT_BALANCE" — look at existing handlers in ApiAdvice for the exact pattern.
  </action>
  <verify>
Run: mvn compile -pl . -q
Expected: BUILD SUCCESS.

Run: mvn test -pl . -Dtest=ClientServiceTest -q (if it exists) — should still pass.

Manual smoke-test after mvn spring-boot:run with a test database:
- POST /api/admin/clients (admin JWT) → 201 → verify client_credit_balance row exists with balance=0 in DB
- Authenticate as new client → GET /v1/credits/balance → 200 {"available_balance":0,"unit":"segments","currency":"SMS_SEGMENT","last_updated_at":"..."}
- GET /v1/credits/ledger → 200 {"entries":[],"page":0,"size":50,"totalElements":0}
  </verify>
  <done>
CreditService, CreditResource, and all contract types compile. ClientService.createClient() inserts a ClientCreditBalance row with balance=0 in the same transaction. GET /v1/credits/balance returns 200 for a new client with available_balance=0. GET /v1/credits/ledger returns 200 with an empty entries array. InsufficientBalanceException is handled by ApiAdvice with HTTP 400 and error_code "INSUFFICIENT_CLIENT_BALANCE".
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

1. mvn compile — BUILD SUCCESS, zero errors
2. mvn spring-boot:run — application starts, Flyway applies V3__credit_ledger.sql successfully (check logs: "Successfully applied 1 migration to schema main")
3. POST /api/admin/clients with admin JWT → verify client_credit_balance row exists: SELECT * FROM main.client_credit_balance WHERE client_id = ?; — row present with balance=0
4. GET /v1/credits/balance (API key auth) → 200 {"available_balance":0,"unit":"segments","currency":"SMS_SEGMENT","last_updated_at":"..."}
5. GET /v1/credits/ledger → 200 {"entries":[],"page":0,"size":50,"totalElements":0}
6. GET /v1/credits/ledger?page=0&size=200 — size clamped to 200, still 200 OK
7. Verify client_credit_balance has UNIQUE constraint on client_id: INSERT duplicate client_id → DB error (constraint enforcement)
8. Verify CreditLedgerRepository.findByClientIdOrderByCreatedDateDesc uses the composite index: EXPLAIN SELECT ... FROM credit_ledger_entry WHERE client_id=? ORDER BY created_date DESC LIMIT 50 — should show Index Scan on idx_ledger_client_date
</verification>

<success_criteria>
- V3__credit_ledger.sql applied by Flyway with no errors
- GET /v1/credits/balance returns 200 with correct JSON shape (available_balance, unit, currency, last_updated_at)
- GET /v1/credits/ledger returns 200 with paginated entries array
- New client always has a client_credit_balance row (balance=0) created atomically in createClient()
- CreditService.applyLedgerEntry() is callable from Plan 02 (TopupService) and Plan 03 (CreditReservationService) — method signature: void applyLedgerEntry(Long clientId, LedgerEntryType type, long amount, String reference)
- InsufficientBalanceException returns HTTP 400 with error_code "INSUFFICIENT_CLIENT_BALANCE"
- Balance query is O(1): SELECT balance FROM client_credit_balance WHERE client_id = ? (no SUM)
</success_criteria>

<output>
After completion, create `.planning/phases/02-credit-ledger-topups/02-01-SUMMARY.md` following the summary template at @~/.claude/get-shit-done/templates/summary.md
</output>
