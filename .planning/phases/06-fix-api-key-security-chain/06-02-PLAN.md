---
phase: 06-fix-api-key-security-chain
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/test/java/com/softropic/sendam/gateway/auth/api/ClientApiKeySecurityIT.java
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "GET /v1/api/keys with a valid Bearer API key (snd_ format) returns HTTP 200"
    - "Response body is a JSON array (may be empty or contain the seeded key)"
    - "The existing negative-path tests (no token, bad token) still return 401"
  artifacts:
    - path: "src/test/java/com/softropic/sendam/gateway/auth/api/ClientApiKeySecurityIT.java"
      provides: "Positive-path IT confirming full API key authentication round-trip"
      exports: ["clientApiKeysWithValidTokenReturns200", "clientApiKeysWithNoTokenReturns401", "clientApiKeysWithRevokedOrInvalidTokenReturns401"]
  key_links:
    - from: "ClientApiKeySecurityIT.clientApiKeysWithValidTokenReturns200"
      to: "GET /v1/api/keys"
      via: "Authorization: Bearer <rawKey> header + ApiKeyAuthenticationFilter"
      pattern: "setBearerAuth.*rawKey"
    - from: "ApiKeyService.createKey"
      to: "client_api_key table"
      via: "JdbcTemplate-seeded client_account row as FK target"
      pattern: "apiKeyService\\.createKey"
---

<objective>
Close the single verification gap from 06-VERIFICATION.md: add the missing positive-path integration test
`clientApiKeysWithValidTokenReturns200` to ClientApiKeySecurityIT.

Purpose: The routing fix (CLIENT_API_KEYS in securityMatcher) is structurally correct and proven by
negative-path tests, but the verifier requires an automated test that proves a valid Bearer API key
produces HTTP 200 from GET /v1/api/keys — completing the end-to-end authentication round-trip
in the test suite.

Output: ClientApiKeySecurityIT.java updated with one new @Test method and supporting
@BeforeEach/@AfterEach fixture setup.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-fix-api-key-security-chain/06-01-SUMMARY.md
@.planning/phases/06-fix-api-key-security-chain/06-VERIFICATION.md

<!-- Key files to read before editing -->
@src/test/java/com/softropic/sendam/gateway/auth/api/ClientApiKeySecurityIT.java
@src/main/java/com/softropic/sendam/gateway/auth/service/ApiKeyService.java
@src/main/java/com/softropic/sendam/gateway/auth/contract/ApiKeyCreationResult.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add positive-path test with client fixture to ClientApiKeySecurityIT</name>
  <files>src/test/java/com/softropic/sendam/gateway/auth/api/ClientApiKeySecurityIT.java</files>
  <action>
    Modify the existing ClientApiKeySecurityIT class (do NOT recreate it — it already has two passing
    tests). Add the following to the class:

    NEW IMPORTS (add to the existing import block):
      import com.softropic.sendam.gateway.auth.service.ApiKeyService;
      import com.softropic.sendam.gateway.auth.contract.ApiKeyCreationResult;
      import org.junit.jupiter.api.AfterEach;
      import org.junit.jupiter.api.BeforeEach;
      import org.springframework.beans.factory.annotation.Autowired;
      import org.springframework.http.ResponseEntity;
      import org.springframework.jdbc.core.JdbcTemplate;
      import org.springframework.transaction.support.TransactionTemplate;
      import java.util.List;

    NEW FIELDS (add after existing @LocalServerPort field):
      @Autowired
      private ApiKeyService apiKeyService;

      @Autowired
      private JdbcTemplate jdbcTemplate;

      @Autowired
      private TransactionTemplate transactionTemplate;

      private static final Long TEST_CLIENT_ID = 999_001L;
      private String rawKey;

    NEW @BeforeEach method (add before the existing tests):
      @BeforeEach
      void seedClient() {
          transactionTemplate.execute(status -> {
              jdbcTemplate.update(
                  "INSERT INTO main.client_account (id, name, status, created_by, created_date, last_modified_by, last_modified_date, request_id) " +
                  "VALUES (?, 'IT Test Client', 'ACTIVE', 'test', NOW(), 'test', NOW(), 'test-req')",
                  TEST_CLIENT_ID
              );
              return null;
          });
          // ApiKeyService.createKey() runs in its own @Transactional — client_account row must exist first.
          ApiKeyCreationResult result = apiKeyService.createKey(TEST_CLIENT_ID, "it-test-key");
          rawKey = result.rawKey();
      }

    NEW @AfterEach method (add after @BeforeEach):
      @AfterEach
      void cleanUp() {
          transactionTemplate.execute(status -> {
              jdbcTemplate.update("DELETE FROM main.client_api_key WHERE client_id = ?", TEST_CLIENT_ID);
              jdbcTemplate.update("DELETE FROM main.client_account WHERE id = ?", TEST_CLIENT_ID);
              return null;
          });
      }

    NEW @Test method (add after the existing two test methods):
      /**
       * GET /v1/api/keys with a valid Bearer API key must return 200.
       *
       * Proves the full authentication round-trip:
       *   1. ApiKeyAuthenticationFilter extracts the raw key from the Bearer token.
       *   2. ApiKeyService.authenticate() hashes the secret with the server pepper and matches
       *      the stored hash in client_api_key.
       *   3. The filter sets clientId as the Authentication principal.
       *   4. ClientApiKeyResource.listKeys() is invoked and returns HTTP 200 with a JSON array.
       */
      @Test
      void clientApiKeysWithValidTokenReturns200() {
          String url = baseUrl() + CLIENT_API_KEYS_URL;

          HttpHeaders headers = jsonHeaders();
          headers.setBearerAuth(rawKey);

          ResponseEntity<List> response = httpTestClient.makeHttpRequest(
              url, HttpMethod.GET, null, headers, List.class
          );

          assertThat(response.getStatusCode()).isEqualTo(HttpStatus.OK);
          assertThat(response.getBody()).isNotNull();
      }

    IMPORTANT CONSTRAINTS:
    - Do NOT change or remove the existing two test methods (clientApiKeysWithNoTokenReturns401,
      clientApiKeysWithRevokedOrInvalidTokenReturns401). They must remain exactly as they are.
    - The @BeforeEach runs before EVERY test method — the negative-path tests will also have a
      client+key seeded, but they do not use `rawKey` so this is harmless. The @AfterEach cleanup
      ensures no cross-test contamination.
    - TEST_CLIENT_ID = 999_001L must not collide with any production or other test data. Choose a
      value far from normal TSID-generated IDs (which are 18-19 digit numbers). 999_001 is safe.
    - apikey.pepper in the IT context defaults to "change-me-in-production" (no APIKEY_PEPPER env var
      in CI). Both createKey() and authenticate() use the same injected pepper value — the round-trip
      is self-consistent without any manual hash computation.
    - The audit log DataIntegrityViolationException (null url) from SecurityAuditListener is a known
      pre-existing issue logged in 06-01-SUMMARY.md. It does not affect test outcomes.
    - ResponseEntity<List> is sufficient for the response type — the test only needs to assert 200
      and non-null body; it does not inspect individual ApiKeyDto fields.
  </action>
  <verify>
    Run: mvn test -pl . -Dtest=ClientApiKeySecurityIT -q
    Expect: BUILD SUCCESS — 3 tests run, 0 failures, 0 errors.
    All three methods must pass:
      - clientApiKeysWithNoTokenReturns401 (existing)
      - clientApiKeysWithRevokedOrInvalidTokenReturns401 (existing)
      - clientApiKeysWithValidTokenReturns200 (new)
  </verify>
  <done>
    ClientApiKeySecurityIT has 3 passing tests.
    clientApiKeysWithValidTokenReturns200 asserts HTTP 200 using a seeded valid API key.
    The two existing negative-path tests still assert HTTP 401.
    No test data persists after @AfterEach cleanup.
  </done>
</task>

</tasks>

<verification>
Run the full IT class to confirm all three tests pass and no regressions:

  mvn test -pl . -Dtest=ClientApiKeySecurityIT -q

Expected output: 3 tests run, 0 failures, 0 errors.

Gap closure confirmation — re-run the verifier after this plan executes. The gap from 06-VERIFICATION.md
was:
  truth: "clientApiKeysWithValidTokenReturns200 test exists in ClientApiKeySecurityIT"
  status: failed (test was absent)

After this plan: that truth is VERIFIED — the test exists and asserts HTTP 200 with a real Bearer key.
</verification>

<success_criteria>
1. ClientApiKeySecurityIT contains clientApiKeysWithValidTokenReturns200 as a @Test method.
2. The method seeds a real client_account and client_api_key via ApiKeyService.createKey().
3. GET /v1/api/keys with that rawKey as Bearer token returns HTTP 200 with a non-null JSON array body.
4. All three tests in ClientApiKeySecurityIT pass: 0 failures.
5. @AfterEach cleanup deletes the seeded rows — no data leak between tests.
6. The gap from 06-VERIFICATION.md (missing positive-path test) is closed.
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-api-key-security-chain/06-02-SUMMARY.md`
</output>
