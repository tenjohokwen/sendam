---
phase: 14-client-apikey-management
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - src/pages/admin/ClientsPage.vue
  - src/components/admin/CreateClientDialog.vue
autonomous: true

must_haves:
  truths:
    - "Admin can see a table of all clients showing ID, name, status, and balance"
    - "Admin can filter the client list by typing in a search box (matches on ID or name)"
    - "Admin can click Register Client, fill in name and optional key label, and submit"
    - "Table shows a loading spinner while clients are being fetched"
    - "Register Client button is disabled and shows loading state while submitting"
    - "Form validates name lazily (on blur): required, min 2 chars, max 100 chars"
    - "On successful client creation, a positive notification appears and the list reloads"
    - "On API error, an error banner appears above the table (for page errors) or form (for dialog errors)"
  artifacts:
    - path: "src/pages/admin/ClientsPage.vue"
      provides: "Client list page with table, search, register button, error handling"
      min_lines: 100
    - path: "src/components/admin/CreateClientDialog.vue"
      provides: "Create client form dialog with validation and error handling"
      min_lines: 80
  key_links:
    - from: "src/pages/admin/ClientsPage.vue"
      to: "src/api/admin/index.js"
      via: "adminApi.getClients() in onMounted"
      pattern: "adminApi\\.getClients"
    - from: "src/pages/admin/ClientsPage.vue"
      to: "src/components/admin/CreateClientDialog.vue"
      via: "v-model showCreateDialog"
      pattern: "CreateClientDialog"
    - from: "src/components/admin/CreateClientDialog.vue"
      to: "src/api/admin/index.js"
      via: "adminApi.createClient(data) on submit"
      pattern: "adminApi\\.createClient"
---

<objective>
Build the Clients admin page (replacing the stub) and the Create Client dialog. Admin can view, search, and register clients.

Purpose: Delivers CLNT-01, CLNT-02, CLNT-03 and establishes the FOUND-05, FOUND-08, UXST-01, UXST-04 patterns that remaining Phase 14 plans follow.
Output: ClientsPage.vue (full implementation) and CreateClientDialog.vue (new component).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/14-client-apikey-management/14-RESEARCH.md
@.planning/phases/14-client-apikey-management/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ClientsPage.vue</name>
  <files>src/pages/admin/ClientsPage.vue</files>
  <action>
Replace the 11-line stub entirely. The page fetches the full client list on mount, supports client-side filtering, and hosts the Create and ApiKeys dialogs (ApiKeys dialog integration is wired in Plan 03 — this plan mounts the component but leaves its v-model stub as `showApiKeysDialog = false` and a "TODO: Plan 03" comment on the "Manage Keys" button click handler).

**Script setup block:**

```js
import { ref, computed, onMounted } from 'vue'
import { useI18n } from 'vue-i18n'
import { useQuasar } from 'quasar'
import { adminApi } from 'src/api/admin'
import { useErrorHandler } from 'src/composables/useErrorHandler'
import { normalizeLongIds } from 'src/utils/longToString'
import CreateClientDialog from 'src/components/admin/CreateClientDialog.vue'

const { t } = useI18n()
const $q = useQuasar()
const { setError, clearError, hasError, errorMessage } = useErrorHandler()

const clients = ref([])
const isLoading = ref(false)
const filterText = ref('')
const showCreateDialog = ref(false)
// ApiKeysDialog wired in Plan 03:
const selectedClient = ref(null)
const showApiKeysDialog = ref(false)

const filteredClients = computed(() => {
  const q = filterText.value.trim().toLowerCase()
  if (!q) return clients.value
  return clients.value.filter(
    (c) => c.id.toLowerCase().includes(q) || c.name.toLowerCase().includes(q)
  )
})

onMounted(loadClients)

async function loadClients() {
  isLoading.value = true
  clearError()
  try {
    const list = await adminApi.getClients()
    clients.value = list.map((c) => normalizeLongIds(c, ['id']))
  } catch (err) {
    setError(err)
  } finally {
    isLoading.value = false
  }
}

function openManageKeys(client) {
  selectedClient.value = client
  showApiKeysDialog.value = true
}

function onClientCreated() {
  loadClients()
}
```

**Template structure:**

```html
<template>
  <q-page padding>
    <div class="row items-center justify-between q-mb-md">
      <div class="text-h5">{{ t('admin.clients.title') }}</div>
      <q-btn
        color="primary"
        icon="add"
        :label="t('admin.clients.registerClient')"
        @click="showCreateDialog = true"
      />
    </div>

    <!-- Search filter -->
    <q-input
      v-model="filterText"
      outlined
      dense
      clearable
      :placeholder="t('admin.clients.filterPlaceholder')"
      class="q-mb-md"
      style="max-width: 400px"
    >
      <template #prepend><q-icon name="search" /></template>
    </q-input>

    <!-- Error banner -->
    <q-banner v-if="hasError" class="bg-negative text-white q-mb-md" rounded>
      {{ errorMessage }}
    </q-banner>

    <!-- Client table -->
    <div class="relative-position">
      <q-markup-table flat bordered>
        <thead>
          <tr>
            <th class="text-left">{{ t('admin.clients.id') }}</th>
            <th class="text-left">{{ t('admin.clients.name') }}</th>
            <th class="text-left">{{ t('admin.clients.status') }}</th>
            <th class="text-right">{{ t('admin.clients.balance') }}</th>
            <th class="text-center">Actions</th>
          </tr>
        </thead>
        <tbody>
          <tr v-if="filteredClients.length === 0 && !isLoading">
            <td colspan="5" class="text-center text-grey">
              {{ t('admin.clients.noClients') }}
            </td>
          </tr>
          <tr v-for="client in filteredClients" :key="client.id">
            <td class="text-left text-mono text-caption">{{ client.id }}</td>
            <td class="text-left">{{ client.name }}</td>
            <td class="text-left">
              <q-badge
                :color="client.status === 'ACTIVE' ? 'positive' : 'grey'"
                :label="t('admin.clients.status' + client.status.charAt(0) + client.status.slice(1).toLowerCase())"
              />
            </td>
            <td class="text-right">{{ client.balance }}</td>
            <td class="text-center">
              <q-btn
                flat
                dense
                color="primary"
                icon="key"
                :label="t('admin.clients.manageKeys')"
                @click="openManageKeys(client)"
              />
            </td>
          </tr>
        </tbody>
      </q-markup-table>

      <!-- Loading overlay -->
      <q-inner-loading :showing="isLoading">
        <q-spinner-dots size="40px" color="primary" />
      </q-inner-loading>
    </div>

    <!-- Dialogs -->
    <CreateClientDialog
      v-model="showCreateDialog"
      @created="onClientCreated"
    />
    <!-- ApiKeysDialog mounted in Plan 03 -->
  </q-page>
</template>
```

Status badge label: Use a computed helper or inline ternary to map `ACTIVE` → `t('admin.clients.statusActive')`, `INACTIVE` → `t('admin.clients.statusInactive')`, `DELETED` → `t('admin.clients.statusDeleted')`. A simple mapping object works well:

```js
const statusLabelMap = {
  ACTIVE: () => t('admin.clients.statusActive'),
  INACTIVE: () => t('admin.clients.statusInactive'),
  DELETED: () => t('admin.clients.statusDeleted')
}
```

Then use `statusLabelMap[client.status]?.() ?? client.status` in the template. Do NOT use the charAt/slice pattern shown above — use the map instead.
  </action>
  <verify>
    Run `grep -n "adminApi.getClients\|normalizeLongIds\|QInnerLoading\|q-inner-loading\|CreateClientDialog\|filterText\|filteredClients" src/pages/admin/ClientsPage.vue` — all should match.
    Run `npm run lint` and verify no errors in ClientsPage.vue.
  </verify>
  <done>
    ClientsPage.vue: loads clients on mount, normalizes Long IDs, supports client-side filter, shows QInnerLoading during fetch, shows error banner on failure, mounts CreateClientDialog, has openManageKeys stub for Plan 03 wiring.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build CreateClientDialog.vue</name>
  <files>src/components/admin/CreateClientDialog.vue</files>
  <action>
Create the `src/components/admin/` directory if it does not exist. Create `CreateClientDialog.vue` as a new file.

**Script setup:**

```js
import { ref, computed } from 'vue'
import { useI18n } from 'vue-i18n'
import { useQuasar } from 'quasar'
import { adminApi } from 'src/api/admin'
import { useErrorHandler } from 'src/composables/useErrorHandler'

const props = defineProps({
  modelValue: { type: Boolean, required: true }
})
const emit = defineEmits(['update:modelValue', 'created'])

const { t } = useI18n()
const $q = useQuasar()
const { setError, clearError, hasError, errorMessage, isValidationError, hasFieldError, getFieldError } = useErrorHandler()

const isSubmitting = ref(false)
const name = ref('')
const keyLabel = ref('')

const dialogVisible = computed({
  get: () => props.modelValue,
  set: (val) => emit('update:modelValue', val)
})

function onClose() {
  name.value = ''
  keyLabel.value = ''
  clearError()
  dialogVisible.value = false
}

async function onSubmit() {
  isSubmitting.value = true
  clearError()
  try {
    const payload = { name: name.value }
    if (keyLabel.value.trim()) payload.keyLabel = keyLabel.value.trim()
    await adminApi.createClient(payload)
    $q.notify({ type: 'positive', message: t('admin.clients.created') })
    emit('created')
    onClose()
  } catch (err) {
    setError(err)
  } finally {
    isSubmitting.value = false
  }
}
```

**Template:**

```html
<template>
  <q-dialog v-model="dialogVisible" persistent @hide="onClose">
    <q-card style="min-width: 400px">
      <q-card-section class="row items-center q-pb-none">
        <div class="text-h6">{{ t('admin.clients.createTitle') }}</div>
        <q-space />
        <q-btn icon="close" flat round dense @click="onClose" />
      </q-card-section>

      <q-card-section>
        <!-- Error banner (non-field errors only) -->
        <q-banner
          v-if="hasError && !isValidationError"
          class="bg-negative text-white q-mb-md"
          rounded
        >
          {{ errorMessage }}
        </q-banner>

        <q-form @submit.prevent="onSubmit">
          <q-input
            v-model="name"
            :label="t('admin.clients.clientName')"
            outlined
            :lazy-rules="true"
            :rules="[
              (val) => !!val || t('validation.required'),
              (val) => val.length >= 2 || t('validation.minLength', { min: 2 }),
              (val) => val.length <= 100 || t('validation.maxLength', { max: 100 })
            ]"
            :error="hasFieldError('name')"
            :error-message="getFieldError('name')"
            class="q-mb-md"
          />
          <q-input
            v-model="keyLabel"
            :label="t('admin.clients.keyLabel')"
            outlined
            :lazy-rules="true"
            :rules="[
              (val) => !val || val.length <= 100 || t('validation.maxLength', { max: 100 })
            ]"
            :error="hasFieldError('keyLabel')"
            :error-message="getFieldError('keyLabel')"
            class="q-mb-md"
          />

          <div class="row justify-end q-gutter-sm">
            <q-btn
              flat
              :label="t('common.cancel')"
              :disable="isSubmitting"
              @click="onClose"
            />
            <q-btn
              type="submit"
              color="primary"
              :label="t('admin.clients.registerClient')"
              :loading="isSubmitting"
              :disable="isSubmitting"
            />
          </div>
        </q-form>
      </q-card-section>
    </q-card>
  </q-dialog>
</template>
```

Key rules:
- `:lazy-rules="true"` on every `q-input` — validates on blur, not on mount (FOUND-08).
- Submit button uses `:loading="isSubmitting"` and `:disable="isSubmitting"` (UXST-04).
- Non-field errors shown in banner; field errors shown inline via `hasFieldError`/`getFieldError` (FOUND-07).
- `keyLabel` rule allows empty value (optional field) — rule only fires if non-empty.
- Check what i18n keys `validation.required`, `validation.minLength`, `validation.maxLength`, `common.cancel` actually exist in the project by reading the i18n files before writing. Use whatever validation/common keys already exist; if none exist for these, use literal fallback strings like `'Required'` rather than missing keys.
  </action>
  <verify>
    Run `grep -n "lazy-rules\|isSubmitting\|adminApi.createClient\|update:modelValue\|hasFieldError" src/components/admin/CreateClientDialog.vue` — all should match.
    Run `npm run lint` — no errors in CreateClientDialog.vue.
  </verify>
  <done>
    CreateClientDialog.vue: lazy-rules on both inputs, submit button disabled+loading during submission, non-field error banner, field-level errors, emits 'created' on success, clears state on close. Does NOT attempt to show raw API key (backend returns null).
  </done>
</task>

</tasks>

<verification>
1. `src/pages/admin/ClientsPage.vue` replaces the stub (> 100 lines).
2. `src/components/admin/CreateClientDialog.vue` is a new file under components/admin/.
3. QInnerLoading visible during client list fetch.
4. Client-side filter computed property filters on both id and name.
5. All Long IDs go through normalizeLongIds before entering clients ref.
6. All q-input elements use :lazy-rules="true".
7. Submit button has :loading and :disable bound to isSubmitting.
8. `npm run lint` and `npm run build` pass with no errors.
</verification>

<success_criteria>
- Navigating to /admin/clients loads a table of clients from GET /api/admin/clients
- Typing in the search box filters the table without making additional API calls
- Clicking Register Client opens a dialog with name (required) and keyLabel (optional) fields
- Submitting with empty name shows a validation error on the name field (lazy — only after blur)
- Successful submission shows a positive toast notification and refreshes the table
- API error during submission shows an error banner inside the dialog
- Submit button cannot be clicked twice (disabled during loading)
</success_criteria>

<output>
After completion, create `.planning/phases/14-client-apikey-management/14-02-SUMMARY.md`
</output>
