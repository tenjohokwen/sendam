---
phase: 17-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/frontend/src/components/admin/DashboardSmsCard.vue
  - src/frontend/src/components/admin/DashboardBillingCard.vue
  - src/frontend/src/components/admin/DashboardSystemCard.vue
autonomous: true

must_haves:
  truths:
    - "DashboardSmsCard renders total_sent, delivered, failed, delivery_rate (formatted as %), total_segments, and a daily breakdown table"
    - "DashboardBillingCard renders active client count, total credits, sms_debit, sms_refund, topup_approved, net_credits_consumed"
    - "DashboardSystemCard renders a colour-coded circuit breaker badge (positive/warning/negative) and provider + webhook stat rows"
    - "All three cards are purely display components — they accept props, make no API calls, and have no loading state of their own"
    - "All display text uses i18n keys from admin.dashboard.*"
  artifacts:
    - path: "src/frontend/src/components/admin/DashboardSmsCard.vue"
      provides: "SMS statistics card"
      min_lines: 50
    - path: "src/frontend/src/components/admin/DashboardBillingCard.vue"
      provides: "Credits and clients card"
      min_lines: 50
    - path: "src/frontend/src/components/admin/DashboardSystemCard.vue"
      provides: "Circuit breaker and provider health card"
      min_lines: 60
  key_links:
    - from: "DashboardSmsCard.vue"
      to: "admin.dashboard.deliveryRate i18n key"
      via: "t('admin.dashboard.deliveryRate')"
      pattern: "admin\\.dashboard\\.deliveryRate"
    - from: "DashboardSystemCard.vue"
      to: "cbColorMap"
      via: ":color prop on QBadge"
      pattern: "cbColorMap"
---

<objective>
Create the three pure-display card components that AdminDashboardPage.vue will render. Each card receives its slice of dashboard data as props and is responsible only for formatting and layout.

Purpose: Decomposing into three focused cards is mandatory — the 250-line limit makes a monolithic dashboard page impossible. Each card owns one concern: SMS stats, billing/credits, or system health.
Output: Three new .vue files in src/frontend/src/components/admin/, each under 100 lines, each receiving typed props and rendering via QCard layout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-dashboard/17-RESEARCH.md
@.planning/phases/17-dashboard/17-01-SUMMARY.md

@src/frontend/src/i18n/en-US/index.js
@src/frontend/src/components/admin/DlrDialog.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DashboardSmsCard.vue</name>
  <files>src/frontend/src/components/admin/DashboardSmsCard.vue</files>
  <action>
Create src/frontend/src/components/admin/DashboardSmsCard.vue as a new file. Props: `smsStats` (Object, required). No API calls. No loading state.

Structure:
- QCard with a QCardSection header showing t('admin.dashboard.smsCard')
- QCardSection with stat rows for: totalSent, delivered, failed, deliveryRate, totalSegments
- Stat row pattern: `<div class="row justify-between q-py-xs"><span class="text-grey-7">{{ label }}</span><span class="text-weight-medium">{{ value }}</span></div>`
- deliveryRate must be formatted: local function `formatPct(rate)` that returns `(rate * 100).toFixed(1) + '%'` when rate != null, else '—'
- A QSeparator between the summary rows and the daily breakdown
- A compact QMarkupTable for `smsStats.daily_breakdown` array (columns: date, total_sent, delivered, failed). Use `v-if="smsStats.daily_breakdown && smsStats.daily_breakdown.length"` to guard the table. Table header labels: t('admin.dashboard.date'), t('admin.dashboard.totalSent'), t('admin.dashboard.delivered'), t('admin.dashboard.failed').
- Script setup with `const { t } = useI18n()` and `const props = defineProps({ smsStats: { type: Object, required: true } })`
- Do NOT use longToString on any field in smsStats — all values are numeric stats, not IDs
- Guard null values on every stat row: `smsStats.total_sent ?? '—'` pattern
  </action>
  <verify>wc -l src/frontend/src/components/admin/DashboardSmsCard.vue — must be under 100. grep -n "formatPct\|daily_breakdown\|admin.dashboard.smsCard" src/frontend/src/components/admin/DashboardSmsCard.vue — all three must appear.</verify>
  <done>File exists, is under 100 lines, has formatPct function, renders daily_breakdown table conditionally, uses admin.dashboard.* i18n keys for all labels.</done>
</task>

<task type="auto">
  <name>Task 2: Create DashboardBillingCard.vue</name>
  <files>src/frontend/src/components/admin/DashboardBillingCard.vue</files>
  <action>
Create src/frontend/src/components/admin/DashboardBillingCard.vue as a new file. Props: `creditSummary` (Object, required), `activeClientCount` (Number, required), `totalCredits` (Number, required). No API calls. No loading state.

Structure:
- QCard with a QCardSection header showing t('admin.dashboard.billingCard')
- QCardSection with stat rows for: activeClientCount, totalCredits, then from creditSummary: sms_debit, sms_refund, topup_approved, net_credits_consumed
- Stat row pattern same as DashboardSmsCard (row justify-between q-py-xs, text-grey-7 label, text-weight-medium value)
- i18n labels: t('admin.dashboard.activeClients'), t('admin.dashboard.totalCredits'), t('admin.dashboard.smsDebit'), t('admin.dashboard.smsRefund'), t('admin.dashboard.topupApproved'), t('admin.dashboard.netConsumed')
- Guard null values: `creditSummary.sms_debit ?? '—'` pattern
- Script setup with `const { t } = useI18n()` and `defineProps({ creditSummary: { type: Object, required: true }, activeClientCount: { type: Number, required: true }, totalCredits: { type: Number, required: true } })`
- totalCredits and activeClientCount are derived numbers from the parent — display them as-is (no longToString, no formatting beyond ?? '—')
  </action>
  <verify>wc -l src/frontend/src/components/admin/DashboardBillingCard.vue — must be under 100. grep -n "activeClientCount\|totalCredits\|admin.dashboard.billingCard" src/frontend/src/components/admin/DashboardBillingCard.vue — all three must appear.</verify>
  <done>File exists, is under 100 lines, accepts three props, renders six stat rows with i18n labels, guards nulls.</done>
</task>

<task type="auto">
  <name>Task 3: Create DashboardSystemCard.vue</name>
  <files>src/frontend/src/components/admin/DashboardSystemCard.vue</files>
  <action>
Create src/frontend/src/components/admin/DashboardSystemCard.vue as a new file. Props: `circuitBreaker` (Object, required), `providerStats` (Object, required), `webhookStats` (Object, required). No API calls. No loading state.

Structure:
- QCard with a QCardSection header showing t('admin.dashboard.systemCard')
- Circuit breaker section (QCardSection):
  - Label: t('admin.dashboard.circuitBreaker') as a text-subtitle2
  - Prominent QBadge: `:color="cbColorMap[circuitBreaker.state] ?? 'grey'"` `:label="cbStateLabel(circuitBreaker.state)"` class="text-subtitle2 q-ml-sm"
  - Stat row for failure_rate_pct: label t('admin.dashboard.failureRate'), value `circuitBreaker.failure_rate_pct != null ? circuitBreaker.failure_rate_pct.toFixed(1) + '%' : '—'`
- QSeparator between circuit breaker and provider sections
- Provider stats section (QCardSection):
  - Header: t('admin.dashboard.providerStats') as text-subtitle2
  - Rows: totalSubmitted (total_submitted), drReceived (dr_received), failedCount (failed_count)
  - Guard nulls with ?? '—'
- QSeparator
- Webhook stats section (QCardSection):
  - Header: t('admin.dashboard.webhookStats') as text-subtitle2
  - Rows: totalAttempts (total_attempts), failureCount (failure_count), exhaustedCount (exhausted_count)
  - Guard nulls with ?? '—'

Script setup:
```js
import { useI18n } from 'vue-i18n'
const { t } = useI18n()

defineProps({
  circuitBreaker: { type: Object, required: true },
  providerStats:  { type: Object, required: true },
  webhookStats:   { type: Object, required: true }
})

const cbColorMap = { CLOSED: 'positive', HALF_OPEN: 'warning', OPEN: 'negative' }

function cbStateLabel(state) {
  const map = {
    CLOSED:    t('admin.dashboard.cbStateClosed'),
    OPEN:      t('admin.dashboard.cbStateOpen'),
    HALF_OPEN: t('admin.dashboard.cbStateHalfOpen'),
  }
  return map[state] ?? state
}
```

Do NOT display failed_calls, successful_calls, not_permitted_calls from circuit breaker response — keep card focused and under 100 lines.
  </action>
  <verify>wc -l src/frontend/src/components/admin/DashboardSystemCard.vue — must be under 100. grep -n "cbColorMap\|cbStateLabel\|admin.dashboard.systemCard" src/frontend/src/components/admin/DashboardSystemCard.vue — all three must appear.</verify>
  <done>File exists, is under 100 lines, has cbColorMap and cbStateLabel, renders circuit breaker badge with correct color mapping, renders provider and webhook stat rows with i18n labels.</done>
</task>

</tasks>

<verification>
1. All three files exist: ls src/frontend/src/components/admin/Dashboard*.vue
2. All three are under 100 lines: wc -l src/frontend/src/components/admin/Dashboard*.vue
3. No API calls inside cards: grep -n "adminApi\|api.get" src/frontend/src/components/admin/Dashboard*.vue — should return nothing
4. i18n used throughout: grep -c "admin.dashboard" src/frontend/src/components/admin/DashboardSmsCard.vue — should be > 5
5. cbColorMap correct values: grep -n "positive\|warning\|negative" src/frontend/src/components/admin/DashboardSystemCard.vue
</verification>

<success_criteria>
- DashboardSmsCard.vue: renders all 6 SMS stat fields, has formatPct function, renders daily_breakdown table guarded by v-if, under 100 lines
- DashboardBillingCard.vue: renders 6 billing/client stats from 3 props, under 100 lines
- DashboardSystemCard.vue: renders circuit breaker QBadge with colour map, provider stats section, webhook stats section, under 100 lines
- All three components: no API calls, no loading state, all text via i18n keys, null-guarded with ?? '—'
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard/17-02-SUMMARY.md` following the summary template.
</output>
