---
phase: 17-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/api/admin/index.js
  - src/frontend/src/i18n/en-US/index.js
  - src/frontend/src/i18n/fr-FR/index.js
autonomous: true

must_haves:
  truths:
    - "adminApi exposes getDeliveryStats, getCreditSummary, getCircuitBreakerHealth, getProviderStats, and getWebhookHealth methods"
    - "All 37 admin.dashboard.* i18n keys are present in both en-US and fr-FR with full parity"
  artifacts:
    - path: "src/frontend/src/api/admin/index.js"
      provides: "Five new adminApi wrapper methods for dashboard endpoints"
      contains: "getDeliveryStats"
    - path: "src/frontend/src/i18n/en-US/index.js"
      provides: "English dashboard i18n keys"
      contains: "admin.dashboard"
    - path: "src/frontend/src/i18n/fr-FR/index.js"
      provides: "French dashboard i18n keys"
      contains: "admin.dashboard"
  key_links:
    - from: "src/frontend/src/api/admin/index.js"
      to: "/api/admin/sms/analytics/delivery-stats"
      via: "getDeliveryStats method"
      pattern: "getDeliveryStats"
    - from: "src/frontend/src/api/admin/index.js"
      to: "/api/admin/health/nexah/circuit-breaker"
      via: "getCircuitBreakerHealth method"
      pattern: "getCircuitBreakerHealth"
---

<objective>
Add the five adminApi wrapper methods needed by the dashboard, and add the full admin.dashboard.* i18n namespace to both locale files.

Purpose: These are the foundational pieces that all three dashboard card components depend on. Keeping them in a single plan ensures the API contract and translation keys land together before any component build begins.
Output: Five new methods appended to adminApi; a new admin.dashboard section in en-US/index.js and fr-FR/index.js with 37 keys each and full parity.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-dashboard/17-RESEARCH.md

@src/frontend/src/api/admin/index.js
@src/frontend/src/i18n/en-US/index.js
@src/frontend/src/i18n/fr-FR/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add five adminApi dashboard methods</name>
  <files>src/frontend/src/api/admin/index.js</files>
  <action>
Append five new methods after the last existing method (getWebhookDeliveries) in src/frontend/src/api/admin/index.js. Follow the existing TICKET-ID comment annotation pattern from Phase 16.

Methods to add:

```js
  // DASH-01: Delivery stats for SMS card
  getDeliveryStats(params = {}) {
    return api.get('/api/admin/sms/analytics/delivery-stats', { params })
  },

  // DASH-01: Credit spend summary for billing card
  getCreditSummary(params = {}) {
    return api.get('/api/admin/credits/summary', { params })
  },

  // DASH-02: Nexah circuit breaker state
  getCircuitBreakerHealth() {
    return api.get('/api/admin/health/nexah/circuit-breaker')
  },

  // DASH-01: Nexah provider send aggregate
  getProviderStats() {
    return api.get('/api/admin/health/nexah/provider-stats')
  },

  // DASH-01: Webhook delivery health aggregate
  getWebhookHealth() {
    return api.get('/api/admin/health/webhooks/stats')
  }
```

Do NOT modify any existing methods. Do NOT add params to getCircuitBreakerHealth or getProviderStats or getWebhookHealth — those endpoints take no query parameters. Close the object correctly (remove trailing comma on last method, keep closing `}` of the export).
  </action>
  <verify>Check that the file has all five new method names: grep -n "getDeliveryStats\|getCreditSummary\|getCircuitBreakerHealth\|getProviderStats\|getWebhookHealth" src/frontend/src/api/admin/index.js</verify>
  <done>Five methods appear in the file. Existing methods (getClients, createClient, getApiKeys, createApiKey, revokeApiKey, getTopupHistory, approveTopup, rejectTopup, getScheduledSms, getDlrForRequest, getWebhookEndpoints, getWebhookDeliveries) are unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin.dashboard i18n namespace to both locales</name>
  <files>
    src/frontend/src/i18n/en-US/index.js
    src/frontend/src/i18n/fr-FR/index.js
  </files>
  <action>
In both locale files, add a new `dashboard` section inside the existing `admin` object, after the `webhooks` section (before the closing `}` of `admin`). Both files must have exactly the same keys.

en-US values:
```js
    dashboard: {
      title: 'System Overview',
      refresh: 'Refresh',
      lastUpdated: 'Last updated: {time}',
      // SMS card
      smsCard: 'SMS Statistics',
      totalSent: 'Total Sent',
      delivered: 'Delivered',
      failed: 'Failed',
      deliveryRate: 'Delivery Rate',
      totalSegments: 'Total Segments',
      dailyBreakdown: 'Daily Breakdown',
      date: 'Date',
      // Billing card
      billingCard: 'Credits & Clients',
      activeClients: 'Active Clients',
      totalCredits: 'Total Credits (XAF)',
      smsDebit: 'SMS Debit',
      smsRefund: 'SMS Refund',
      topupApproved: 'Topup Approved',
      netConsumed: 'Net Consumed',
      // System health card
      systemCard: 'System Health',
      circuitBreaker: 'Circuit Breaker',
      cbStateClosed: 'Closed (Healthy)',
      cbStateOpen: 'Open (Tripped)',
      cbStateHalfOpen: 'Half-Open (Recovering)',
      failureRate: 'Failure Rate',
      providerStats: 'Provider (Nexah)',
      totalSubmitted: 'Submitted',
      drReceived: 'DRs Received',
      failedCount: 'Failed',
      webhookStats: 'Webhooks',
      totalAttempts: 'Total Attempts',
      failureCount: 'Failures',
      exhaustedCount: 'Exhausted'
    }
```

fr-FR values (same keys, French translations):
```js
    dashboard: {
      title: 'Vue d\'ensemble du systeme',
      refresh: 'Actualiser',
      lastUpdated: 'Mis a jour : {time}',
      // SMS card
      smsCard: 'Statistiques SMS',
      totalSent: 'Total envoye',
      delivered: 'Livre',
      failed: 'Echoue',
      deliveryRate: 'Taux de livraison',
      totalSegments: 'Total segments',
      dailyBreakdown: 'Historique quotidien',
      date: 'Date',
      // Billing card
      billingCard: 'Credits et clients',
      activeClients: 'Clients actifs',
      totalCredits: 'Total credits (XAF)',
      smsDebit: 'Debit SMS',
      smsRefund: 'Remboursement SMS',
      topupApproved: 'Recharge approuvee',
      netConsumed: 'Net consomme',
      // System health card
      systemCard: 'Sante du systeme',
      circuitBreaker: 'Disjoncteur',
      cbStateClosed: 'Ferme (Sain)',
      cbStateOpen: 'Ouvert (Declenche)',
      cbStateHalfOpen: 'Semi-ouvert (En recuperation)',
      failureRate: 'Taux d\'echec',
      providerStats: 'Fournisseur (Nexah)',
      totalSubmitted: 'Soumis',
      drReceived: 'DRs recus',
      failedCount: 'Echoue',
      webhookStats: 'Webhooks',
      totalAttempts: 'Total tentatives',
      failureCount: 'Echecs',
      exhaustedCount: 'Epuise'
    }
```

Insert the `dashboard` key after `webhooks: { ... }` inside the `admin` object. Do not touch any other keys.
  </action>
  <verify>grep -c "dashboard" src/frontend/src/i18n/en-US/index.js && grep -c "dashboard" src/frontend/src/i18n/fr-FR/index.js — both should show at least 1. Then: grep -n "cbStateClosed\|exhaustedCount\|lastUpdated" src/frontend/src/i18n/en-US/index.js src/frontend/src/i18n/fr-FR/index.js — all six lines should appear (3 per file).</verify>
  <done>Both locale files have an admin.dashboard section with 33 keys each. Key count matches between en-US and fr-FR (full parity). No existing keys are modified.</done>
</task>

</tasks>

<verification>
1. `grep -n "getDeliveryStats\|getCreditSummary\|getCircuitBreakerHealth\|getProviderStats\|getWebhookHealth" src/frontend/src/api/admin/index.js` — returns 5 lines
2. `grep -n "admin.dashboard\|cbStateClosed\|exhaustedCount" src/frontend/src/i18n/en-US/index.js` — all present
3. `grep -n "admin.dashboard\|cbStateClosed\|exhaustedCount" src/frontend/src/i18n/fr-FR/index.js` — all present
4. Both locale files have the same key count inside dashboard (count keys in each)
</verification>

<success_criteria>
- Five new adminApi methods are appended to src/frontend/src/api/admin/index.js without modifying existing methods
- admin.dashboard namespace exists in both en-US and fr-FR with identical keys and locale-appropriate values
- No lint or syntax errors in either file (valid JS object literals)
</success_criteria>

<output>
After completion, create `.planning/phases/17-dashboard/17-01-SUMMARY.md` following the summary template.
</output>
