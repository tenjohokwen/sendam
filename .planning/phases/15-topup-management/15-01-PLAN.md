---
phase: 15-topup-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/frontend/src/api/admin/index.js
  - src/frontend/src/i18n/en-US/index.js
  - src/frontend/src/i18n/fr-FR/index.js
autonomous: true

must_haves:
  truths:
    - "adminApi exposes getTopupHistory(), approveTopup(topupId), rejectTopup(topupId)"
    - "en-US locale has a complete admin.topups.* section with all required keys"
    - "fr-FR locale has a complete admin.topups.* section with matching key parity"
  artifacts:
    - path: "src/frontend/src/api/admin/index.js"
      provides: "Three topup API methods on adminApi"
      contains: "getTopupHistory"
    - path: "src/frontend/src/i18n/en-US/index.js"
      provides: "English topup i18n keys"
      contains: "admin.topups"
    - path: "src/frontend/src/i18n/fr-FR/index.js"
      provides: "French topup i18n keys"
      contains: "admin.topups"
  key_links:
    - from: "adminApi.approveTopup"
      to: "PUT /api/admin/topups/{topup_id}/approve"
      via: "api.put with top_XXX formatted id"
      pattern: "api\\.put.*topups.*approve"
    - from: "adminApi.getTopupHistory"
      to: "GET /api/admin/topups/history"
      via: "api.get with optional params object"
      pattern: "api\\.get.*topups/history"
---

<objective>
Add three topup API methods to adminApi and add a complete admin.topups i18n section to both locales.

Purpose: Plan 02 (TopupsPage implementation) depends on both the API module and i18n keys. Separating them into Wave 1 keeps the page plan clean and ensures the foundational pieces are independently verifiable.
Output: Extended adminApi module and two updated locale files with full admin.topups key coverage.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-topup-management/15-RESEARCH.md

# Existing file to extend — read before editing
@src/frontend/src/api/admin/index.js
@src/frontend/src/i18n/en-US/index.js
@src/frontend/src/i18n/fr-FR/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add topup API methods to adminApi</name>
  <files>src/frontend/src/api/admin/index.js</files>
  <action>
    Append three new methods to the adminApi object in `src/frontend/src/api/admin/index.js`.
    Do NOT replace existing methods — add after revokeApiKey().

    Methods to add:

    ```
    getTopupHistory(params = {}) {
      // params may include: topupStatus, clientId, from, to — all optional
      return api.get('/api/admin/topups/history', { params })
    },
    approveTopup(topupId) {
      // topupId MUST be in "top_XXX" format — raw numeric id will return 404
      return api.put(`/api/admin/topups/${topupId}/approve`)
    },
    rejectTopup(topupId) {
      return api.put(`/api/admin/topups/${topupId}/reject`)
    }
    ```

    Note: The backend endpoint paths are `/api/admin/topups/{topup_id}/approve` and
    `/api/admin/topups/{topup_id}/reject` where `{topup_id}` must be the string
    `"top_" + raw_id` (e.g. "top_1234"). Using just the numeric id returns 404.
  </action>
  <verify>
    Read the file back and confirm:
    1. getTopupHistory, approveTopup, rejectTopup are all present
    2. No existing methods (getClients, createClient, getApiKeys, createApiKey, revokeApiKey) were removed
    3. getTopupHistory passes `{ params }` as the second argument to api.get
    4. approveTopup uses template literal: `/api/admin/topups/${topupId}/approve`
  </verify>
  <done>adminApi has 8 total methods: 5 existing + 3 new topup methods. File is syntactically valid JS.</done>
</task>

<task type="auto">
  <name>Task 2: Add admin.topups i18n keys to both locales</name>
  <files>
    src/frontend/src/i18n/en-US/index.js
    src/frontend/src/i18n/fr-FR/index.js
  </files>
  <action>
    Add a `topups` section inside the existing `admin` object in both locale files.
    Insert it after the `apiKeys` block and before the closing brace of `admin`.

    English (en-US) — add to admin object:
    ```
    topups: {
      title: 'Top-up Requests',
      tabPending: 'Pending',
      tabHistory: 'History',
      id: 'Topup ID',
      clientId: 'Client ID',
      amount: 'Amount (XAF)',
      transactionId: 'Transaction ID',
      paymentType: 'Payment Type',
      accountNumber: 'Account Number',
      requestDate: 'Requested',
      processedDate: 'Processed',
      status: 'Status',
      statusPending: 'Pending',
      statusApproved: 'Approved',
      statusRejected: 'Rejected',
      approve: 'Approve',
      reject: 'Reject',
      approved: 'Top-up approved — client balance updated',
      rejected: 'Top-up rejected',
      noPending: 'No pending top-up requests',
      noHistory: 'No top-up history found',
      topupAlreadyProcessed: 'This top-up has already been processed'
    }
    ```

    French (fr-FR) — add to admin object (note: no accents/special chars per existing project
    convention — the existing fr-FR file consistently omits diacritics, follow the same style):
    ```
    topups: {
      title: 'Demandes de recharge',
      tabPending: 'En attente',
      tabHistory: 'Historique',
      id: 'ID recharge',
      clientId: 'ID client',
      amount: 'Montant (XAF)',
      transactionId: 'ID transaction',
      paymentType: 'Mode de paiement',
      accountNumber: 'Numero de compte',
      requestDate: 'Demandee le',
      processedDate: 'Traitee le',
      status: 'Statut',
      statusPending: 'En attente',
      statusApproved: 'Approuvee',
      statusRejected: 'Rejetee',
      approve: 'Approuver',
      reject: 'Rejeter',
      approved: 'Recharge approuvee — solde client mis a jour',
      rejected: 'Recharge rejetee',
      noPending: 'Aucune demande de recharge en attente',
      noHistory: 'Aucun historique de recharge',
      topupAlreadyProcessed: 'Cette recharge a deja ete traitee'
    }
    ```

    Verify the `admin` object still has: title, nav, accessDenied, clients, apiKeys, topups — all present.
    Verify the file remains syntactically valid JS (no trailing commas before `}`).
  </action>
  <verify>
    1. Both files: `admin.topups` section exists with exactly 21 keys listed above
    2. en-US: `admin.topups.approve` === 'Approve'
    3. fr-FR: `admin.topups.approve` === 'Approuver'
    4. en-US: `admin.topups.topupAlreadyProcessed` exists
    5. fr-FR: `admin.topups.topupAlreadyProcessed` exists
    6. Existing keys (admin.clients.*, admin.apiKeys.*) are untouched
  </verify>
  <done>Both locale files have a complete admin.topups section with 21 keys each. Full en-US/fr-FR parity confirmed.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `src/frontend/src/api/admin/index.js` exports adminApi with 3 new methods
2. `src/frontend/src/i18n/en-US/index.js` has admin.topups with 21 keys
3. `src/frontend/src/i18n/fr-FR/index.js` has admin.topups with 21 keys
4. No syntax errors in any file (all objects properly closed)
</verification>

<success_criteria>
- adminApi.getTopupHistory({ topupStatus: 'PENDING_APPROVAL' }) would call GET /api/admin/topups/history?topupStatus=PENDING_APPROVAL
- adminApi.approveTopup('top_1234') would call PUT /api/admin/topups/top_1234/approve
- t('admin.topups.approved') resolves in both en-US and fr-FR without fallback
- t('admin.topups.topupAlreadyProcessed') resolves in both locales
</success_criteria>

<output>
After completion, create `.planning/phases/15-topup-management/15-01-SUMMARY.md`
</output>
