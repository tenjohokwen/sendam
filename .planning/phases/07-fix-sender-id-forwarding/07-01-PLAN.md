---
phase: 07-fix-sender-id-forwarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchService.java
  - src/test/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchServiceTest.java
autonomous: true

must_haves:
  truths:
    - "When a client sends SMS with a custom sender ID, Nexah receives that sender ID (not the global account sender)"
    - "When a client sends SMS, NexahDispatchService no longer reads nexahProperties.senderid() for the senderid field"
    - "Existing behavior is preserved: recipients transition to SUBMITTED and the parent request transitions to SUBMITTED on success"
  artifacts:
    - path: "src/main/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchService.java"
      provides: "Corrected dispatch() — senderid sourced from request.getSender()"
      contains: "request.getSender()"
    - path: "src/test/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchServiceTest.java"
      provides: "Unit test asserting forwarded sender ID"
      contains: "senderid"
  key_links:
    - from: "NexahDispatchService.dispatch()"
      to: "NexahSendRequest constructor"
      via: "request.getSender()"
      pattern: "request\\.getSender\\(\\)"
---

<objective>
Close the SMS-01 correctness gap (WIRING-1 from v1.0 milestone audit): replace
nexahProperties.senderid() with request.getSender() in NexahDispatchService.dispatch() so that
the sender ID the client specified is the one forwarded to Nexah.

Purpose: The sender field on SendRequest is populated from the client request and persisted, but
was silently ignored during dispatch. Every outbound SMS used the global account sender instead.

Output:
  - NexahDispatchService.java with one-line fix
  - NexahDispatchServiceTest.java with updated + new assertions confirming the forwarded sender ID
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/main/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchService.java
@src/main/java/com/softropic/sendam/gateway/provider/nexah/contract/NexahSendRequest.java
@src/main/java/com/softropic/sendam/gateway/sms/repo/SendRequest.java
@src/test/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchServiceTest.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NexahDispatchService — forward request.getSender() to Nexah</name>
  <files>src/main/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchService.java</files>
  <action>
    In dispatch(), change line 63 of the NexahSendRequest constructor call from:
        nexahProperties.senderid(),
    to:
        request.getSender(),

    That is the complete change. Do NOT remove the nexahProperties field — it is still
    used for nexahProperties.user() and nexahProperties.password() on lines 61-62.

    The SendRequest entity already has a getSender() accessor (Lombok @Getter on the
    String sender field). No import changes are required.
  </action>
  <verify>
    grep -n "request.getSender()" src/main/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchService.java
    # Must return the line inside the NexahSendRequest constructor block.

    grep -n "nexahProperties.senderid()" src/main/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchService.java
    # Must return no output (the old call is gone).
  </verify>
  <done>
    NexahDispatchService.java contains request.getSender() in the constructor call and
    no longer calls nexahProperties.senderid().
  </done>
</task>

<task type="auto">
  <name>Task 2: Update unit tests — assert forwarded sender ID, remove stale senderid stub</name>
  <files>src/test/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchServiceTest.java</files>
  <action>
    Two changes to the existing test file:

    1. Remove the nexahProperties.senderid() stub from dispatch_success() and
       dispatch_partialSuccess() — those stubs are now stale and will cause an
       UnnecessaryStubbingException under Mockito's strict mode.

    2. In dispatch_success(), capture the NexahSendRequest argument passed to
       nexahClient.sendSms() using ArgumentCaptor and assert that
       capturedRequest.senderid() equals the sender on the SendRequest fixture.

    Example for (2) — add after the nexahDispatchService.dispatch(request) call:
        ArgumentCaptor<NexahSendRequest> captor = ArgumentCaptor.forClass(NexahSendRequest.class);
        verify(nexahClient).sendSms(captor.capture());
        assertThat(captor.getValue().senderid()).isEqualTo("MYAPP");

    For this to work, build the SendRequest fixture with sender set:
        SendRequest request = SendRequest.builder()
                .id(1L)
                .sendRequestId("req-123")
                .sender("MYAPP")
                .message("test")
                .build();

    The dispatch_partialSuccess() test also stubs nexahProperties.senderid() — remove
    that stub. That test uses any() for the sendSms call so no captor is needed there,
    but set sender on its fixture too so the service call does not NPE on getSender().

    dispatch_providerUnavailable() and dispatch_emptyResponse() do not stub
    nexahProperties.senderid() and do not use sender in assertions — just ensure their
    fixtures also have .sender("MYAPP") so getSender() returns a non-null value.

    Add the import:
        import org.mockito.ArgumentCaptor;
  </action>
  <verify>
    ./mvnw test -pl . -Dtest=NexahDispatchServiceTest -q
    # All 4 tests must pass with exit code 0. No UnnecessaryStubbingException.
  </verify>
  <done>
    NexahDispatchServiceTest passes all 4 tests. dispatch_success verifies that the
    NexahSendRequest.senderid() equals the sender field from the SendRequest fixture
    ("MYAPP"), not the nexahProperties value.
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
  ./mvnw test -q

Expected: BUILD SUCCESS, all tests pass.

Spot-check the fix is wired correctly:
  grep -n "request.getSender()" src/main/java/com/softropic/sendam/gateway/provider/nexah/service/NexahDispatchService.java
  # Returns exactly one hit inside the NexahSendRequest constructor call.
</verification>

<success_criteria>
- NexahDispatchService.dispatch() passes request.getSender() as the senderid argument to NexahSendRequest
- nexahProperties.senderid() is not called anywhere in NexahDispatchService
- NexahDispatchServiceTest.dispatch_success() captures the NexahSendRequest and asserts senderid() == "MYAPP"
- All 4 unit tests in NexahDispatchServiceTest pass
- Full ./mvnw test passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/07-fix-sender-id-forwarding/07-01-SUMMARY.md`
</output>
