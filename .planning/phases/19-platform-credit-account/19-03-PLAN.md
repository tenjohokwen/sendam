---
phase: 19-platform-credit-account
plan: 03
type: execute
wave: 3
depends_on:
  - 19-01
  - 19-02
files_modified:
  - src/main/java/com/softropic/sendam/gateway/billing/api/AdminPlatformCreditResource.java
  - src/main/java/com/softropic/sendam/security/config/AppEndpoints.java
  - src/main/java/com/softropic/sendam/security/api/ApiAdvice.java
  - src/main/java/com/softropic/sendam/gateway/audit/contract/AuditEventType.java
  - src/main/java/com/softropic/sendam/gateway/billing/service/TopupService.java
autonomous: true

must_haves:
  truths:
    - "POST /api/admin/platform/credits/purchases records a Nexah purchase and returns 201 with new balance"
    - "GET /api/admin/platform/credits/balance returns the current platform balance"
    - "GET /api/admin/platform/credits/ledger returns paginated ledger history, filterable by type query param"
    - "All three endpoints reject unauthenticated requests (401) and non-admin requests (403)"
    - "Approving a client top-up atomically debits the platform balance in the same transaction"
    - "Top-up approval returns 422 INSUFFICIENT_PLATFORM_BALANCE when platform balance would go negative"
    - "Client credit write is rolled back when platform balance is insufficient (atomic transaction)"
  artifacts:
    - path: "src/main/java/com/softropic/sendam/gateway/billing/api/AdminPlatformCreditResource.java"
      provides: "Three admin REST endpoints for platform credit"
      contains: "@RequestMapping(\"/api/admin/platform/credits\")"
    - path: "src/main/java/com/softropic/sendam/security/config/AppEndpoints.java"
      provides: "ADMIN_PLATFORM_CREDITS constant + SECURED_MAPPINGS entry"
      contains: "ADMIN_PLATFORM_CREDITS"
    - path: "src/main/java/com/softropic/sendam/security/api/ApiAdvice.java"
      provides: "@ExceptionHandler for InsufficientPlatformBalanceException → HTTP 422"
      contains: "InsufficientPlatformBalanceException"
    - path: "src/main/java/com/softropic/sendam/gateway/audit/contract/AuditEventType.java"
      provides: "NEXAH_PURCHASE_RECORDED audit event type"
      contains: "NEXAH_PURCHASE_RECORDED"
    - path: "src/main/java/com/softropic/sendam/gateway/billing/service/TopupService.java"
      provides: "Modified approve() that debits platform balance"
      contains: "platformCreditService"
  key_links:
    - from: "AdminPlatformCreditResource.recordPurchase()"
      to: "PlatformCreditService.recordNexahPurchase()"
      via: "direct method call"
      pattern: "platformCreditService.recordNexahPurchase"
    - from: "TopupService.approve()"
      to: "PlatformCreditService.applyLedgerEntry()"
      via: "call after creditService.applyLedgerEntry — same @Transactional transaction"
      pattern: "platformCreditService.applyLedgerEntry"
    - from: "AppEndpoints.SECURED_MAPPINGS"
      to: "ADMIN_PLATFORM_CREDITS = \"/api/admin/platform/credits/**\""
      via: "Map.entry in static block"
      pattern: "ADMIN_PLATFORM_CREDITS"
    - from: "ApiAdvice"
      to: "InsufficientPlatformBalanceException"
      via: "@ExceptionHandler → HTTP 422"
      pattern: "HttpStatus.UNPROCESSABLE_ENTITY"
---

<objective>
Wire up the REST layer, security registration, exception handling, and TopupService modification that complete Phase 19. After this plan, all seven PLAT requirements are satisfied.

Purpose: Plan 01 built the schema+entities, Plan 02 built the service. This plan exposes the service via REST (three admin endpoints), registers the new URL pattern in the security filter chain, maps the new exception to HTTP 422, adds the audit event type, and modifies TopupService.approve() to atomically debit the platform balance.
Output: 1 new REST controller + 4 modified files — all Phase 19 requirements satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-platform-credit-account/19-RESEARCH.md
@.planning/phases/19-platform-credit-account/19-01-SUMMARY.md
@.planning/phases/19-platform-credit-account/19-02-SUMMARY.md

# Files being modified
@src/main/java/com/softropic/sendam/gateway/billing/service/TopupService.java
@src/main/java/com/softropic/sendam/security/config/AppEndpoints.java
@src/main/java/com/softropic/sendam/security/api/ApiAdvice.java
@src/main/java/com/softropic/sendam/gateway/audit/contract/AuditEventType.java

# Reference for controller pattern
@src/main/java/com/softropic/sendam/gateway/billing/api/AdminTopupResource.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: AdminPlatformCreditResource + security wiring + ApiAdvice + AuditEventType</name>
  <files>
    src/main/java/com/softropic/sendam/gateway/billing/api/AdminPlatformCreditResource.java
    src/main/java/com/softropic/sendam/security/config/AppEndpoints.java
    src/main/java/com/softropic/sendam/security/api/ApiAdvice.java
    src/main/java/com/softropic/sendam/gateway/audit/contract/AuditEventType.java
  </files>
  <action>
**AdminPlatformCreditResource.java** (new file):
Package: com.softropic.sendam.gateway.billing.api
Annotations: @RestController, @RequestMapping("/api/admin/platform/credits"), @PreAuthorize("hasRole('ADMIN')"), @RequiredArgsConstructor, @Slf4j

Inject: PlatformCreditService platformCreditService

Three endpoints:

1. POST /purchases — record a Nexah credit purchase
   @PostMapping("/purchases")
   @ResponseStatus(HttpStatus.CREATED)
   public RecordNexahPurchaseResponse recordPurchase(@Valid @RequestBody RecordNexahPurchaseRequest request) {
       log.info("Admin recording Nexah purchase: amount={}", request.amount());
       return platformCreditService.recordNexahPurchase(request);
   }

2. GET /balance — query current platform balance
   @GetMapping("/balance")
   public PlatformBalanceResponse getBalance() {
       return platformCreditService.getBalance();
   }

3. GET /ledger — browse ledger history with optional type filter
   @GetMapping("/ledger")
   public PlatformLedgerHistoryResponse getLedgerHistory(
           @RequestParam(required = false) PlatformLedgerEntryType type,
           @RequestParam(defaultValue = "0") int page,
           @RequestParam(defaultValue = "50") int size) {
       return platformCreditService.getLedgerHistory(type, page, size);
   }

Imports: all contract classes from billing.contract, PlatformCreditService, Spring annotations (ResponseStatus, HttpStatus, Valid, RequestBody, GetMapping, PostMapping, RequestMapping, RestController, RequestParam), PreAuthorize, RequiredArgsConstructor, Slf4j.

**AppEndpoints.java** (modification):
Read the current file first. Two changes:
1. Add constant after ADMIN_WEBHOOK_MONITOR line: public static final String ADMIN_PLATFORM_CREDITS = "/api/admin/platform/credits/**";
2. In the SECURED_MAPPINGS static block, add one new Map.entry AFTER the existing ADMIN_WEBHOOK_MONITOR entry:
   Map.entry(ADMIN_PLATFORM_CREDITS, new String[]{AuthoritiesConstants.ADMIN})
IMPORTANT: Map.ofEntries() cannot exceed 10 entries without expanding to a different form. Count existing entries before adding. The current map has 13 entries (SECURED, SECURED_API, ACTUATOR, REFRESH, ADMIN_CLIENTS, ADMIN_TOPUPS, ADMIN_API_KEYS, ADMIN_ANALYTICS, ADMIN_CREDITS, ADMIN_HEALTH, ADMIN_AUDIT, ADMIN_SMS_MONITOR, ADMIN_WEBHOOK_MONITOR). Map.ofEntries() supports up to any number of entries — it accepts varargs. Adding a 14th is safe.

**ApiAdvice.java** (modification):
Add one new @ExceptionHandler method after the existing insufficientBalanceHandler method:

    /**
     * Handles InsufficientPlatformBalanceException thrown when approving a client top-up
     * would reduce the platform balance below zero. Returns HTTP 422 — distinguishes
     * from InsufficientBalanceException (client balance, HTTP 400).
     *
     * @param exception InsufficientPlatformBalanceException with currentBalance, requestedAmount
     * @return 422 Unprocessable Entity with error_code INSUFFICIENT_PLATFORM_BALANCE
     */
    @ExceptionHandler(InsufficientPlatformBalanceException.class)
    @ResponseStatus(HttpStatus.UNPROCESSABLE_ENTITY)
    public ErrorDto insufficientPlatformBalanceHandler(final InsufficientPlatformBalanceException exception) {
        final String defaultMsg = "Platform balance is insufficient to approve this top-up.";
        return logErrorAndReturnDTO(exception, defaultMsg, "INSUFFICIENT_PLATFORM_BALANCE");
    }

Add import: import com.softropic.sendam.gateway.billing.contract.InsufficientPlatformBalanceException;
Place import with the other billing contract imports near the top of the import block.

**AuditEventType.java** (modification):
Add NEXAH_PURCHASE_RECORDED to the enum after TOPUP_REJECTED:
    // PLAT-01: Admin records a Nexah credit purchase
    NEXAH_PURCHASE_RECORDED,
  </action>
  <verify>
./mvnw compile exits 0. AdminPlatformCreditResource.java exists. AppEndpoints.java contains "ADMIN_PLATFORM_CREDITS". ApiAdvice.java contains "InsufficientPlatformBalanceException". AuditEventType.java contains "NEXAH_PURCHASE_RECORDED".
  </verify>
  <done>
REST controller has three endpoints at /api/admin/platform/credits/purchases, /balance, /ledger. ADMIN_PLATFORM_CREDITS is registered in SECURED_MAPPINGS with ROLE_ADMIN. ApiAdvice maps InsufficientPlatformBalanceException to HTTP 422. AuditEventType has NEXAH_PURCHASE_RECORDED.
  </done>
</task>

<task type="auto">
  <name>Task 2: Modify TopupService.approve() to atomically debit platform balance</name>
  <files>src/main/java/com/softropic/sendam/gateway/billing/service/TopupService.java</files>
  <action>
Modify TopupService.java to inject PlatformCreditService and call it inside approve().

**Inject PlatformCreditService:**
Add to the @RequiredArgsConstructor field list (Lombok generates the constructor):
    private final PlatformCreditService platformCreditService;

Place after the existing `private final ApplicationEventPublisher eventPublisher;` line.

**Modify approve() method:**
The current approve() method (lines ~131-156) calls creditService.applyLedgerEntry() then sets entity fields and saves. Insert one call AFTER the creditService.applyLedgerEntry() call and BEFORE entity.setTopupStatus(TopupStatus.APPROVED):

    // Debit platform balance — same transaction as client credit. Lock order: topup row (above) → client
    // credit balance (inside creditService.applyLedgerEntry) → platform balance (here). Never invert.
    // Throws InsufficientPlatformBalanceException (HTTP 422) if platform balance would go negative,
    // which rolls back the entire transaction including the client credit write above.
    platformCreditService.applyLedgerEntry(
        PlatformLedgerEntryType.TOPUP_DEBIT,
        -entity.getAmount(),   // negative: debit from platform
        topupId
    );

The full modified approve() sequence must be:
1. findByIdForUpdate(id) — acquire topup row lock
2. guard: entity.getTopupStatus() != PENDING_APPROVAL → throw TopupAlreadyProcessedException
3. creditService.applyLedgerEntry(clientId, TOPUP_APPROVED, +amount, topupId) — credits client balance (acquires client balance lock)
4. platformCreditService.applyLedgerEntry(TOPUP_DEBIT, -amount, topupId) — debits platform balance (acquires platform balance lock)
5. entity.setTopupStatus(APPROVED); entity.setApprovedAt(Instant.now()); topupRepository.save(entity)
6. eventPublisher.publishEvent(TOPUP_APPROVED audit event)
7. log + return

Add import at top of file: import com.softropic.sendam.gateway.billing.contract.PlatformLedgerEntryType;

Also update the Javadoc on approve() to add:
 * @throws InsufficientPlatformBalanceException if approving this top-up would reduce the platform
 *                                               balance below zero; the entire transaction rolls back

Do NOT change reject(), createTopup(), getTopupStatus(), or any other method.
Do NOT add @Transactional to applyLedgerEntry calls — the existing @Transactional on the approve() method (inherited from class-level @Transactional) propagates to both CreditService and PlatformCreditService, so all three lock acquisitions participate in one transaction.
  </action>
  <verify>
./mvnw compile exits 0. TopupService.java contains "platformCreditService". The field declaration "private final PlatformCreditService platformCreditService" exists. The approve() body contains "PlatformLedgerEntryType.TOPUP_DEBIT" and "-entity.getAmount()".
  </verify>
  <done>
TopupService.approve() calls platformCreditService.applyLedgerEntry(TOPUP_DEBIT, -amount, topupId) after creditService.applyLedgerEntry(). Both calls are within the same @Transactional transaction. If InsufficientPlatformBalanceException is thrown, the entire transaction rolls back (client credit write is undone). The lock order comment is present.
  </done>
</task>

</tasks>

<verification>
1. ./mvnw compile exits 0 — no compilation errors across all modified files
2. ./mvnw test exits 0 (or no new failures)
3. Integration smoke tests (manual curl against running app):
   a. POST /api/admin/platform/credits/purchases with valid admin JWT + body {"amount": 10000} → 201 with newBalance=10000
   b. GET /api/admin/platform/credits/balance with valid admin JWT → 200 with balance=10000
   c. GET /api/admin/platform/credits/ledger with valid admin JWT → 200 with one entry of type NEXAH_PURCHASE
   d. GET /api/admin/platform/credits/ledger?type=NEXAH_PURCHASE → same one entry
   e. GET /api/admin/platform/credits/ledger?type=TOPUP_DEBIT → empty entries list
   f. Approve a pending top-up larger than platform balance → 422 INSUFFICIENT_PLATFORM_BALANCE; platform balance unchanged; client balance unchanged (transaction rolled back)
   g. Approve a pending top-up within platform balance → 200; platform balance decreases; client balance increases
   h. All three platform endpoints without JWT → 401
   i. All three platform endpoints with client (non-admin) JWT → 403
</verification>

<success_criteria>
- AdminPlatformCreditResource exposes three endpoints under /api/admin/platform/credits/
- ADMIN_PLATFORM_CREDITS is registered in AppEndpoints.SECURED_MAPPINGS with ROLE_ADMIN authority
- ApiAdvice maps InsufficientPlatformBalanceException to HTTP 422 UNPROCESSABLE_ENTITY
- AuditEventType.NEXAH_PURCHASE_RECORDED exists
- TopupService.approve() debits platform balance in the same transaction as the client credit
- Lock order (topup row → client balance → platform balance) is documented in comments
- All seven PLAT requirements (PLAT-01 through PLAT-07) are satisfied
</success_criteria>

<output>
After completion, create .planning/phases/19-platform-credit-account/19-03-SUMMARY.md
</output>
