---
phase: 19-platform-credit-account
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/resources/db/migration/V11__platform_credit_account.sql
  - src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditBalance.java
  - src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditBalanceRepository.java
  - src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditLedgerEntry.java
  - src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditLedgerRepository.java
autonomous: true

must_haves:
  truths:
    - "Two new tables exist in the main schema: platform_credit_balance and platform_credit_ledger_entry"
    - "A singleton row exists in platform_credit_balance with balance = 0"
    - "JPA entities map to those tables and compile cleanly"
    - "The balance repository exposes a SELECT FOR UPDATE query with 2000ms lock timeout"
    - "The ledger repository exposes a paginated query filterable by entry type"
  artifacts:
    - path: "src/main/resources/db/migration/V11__platform_credit_account.sql"
      provides: "DDL for both tables + singleton INSERT"
    - path: "src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditBalance.java"
      provides: "JPA entity for singleton balance row"
      contains: "@Entity"
    - path: "src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditBalanceRepository.java"
      provides: "findForUpdate() + findBalance()"
      contains: "PESSIMISTIC_WRITE"
    - path: "src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditLedgerEntry.java"
      provides: "JPA entity for append-only ledger"
      contains: "@Entity"
    - path: "src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditLedgerRepository.java"
      provides: "findByOptionalType paginated query"
      contains: "Page"
  key_links:
    - from: "PlatformCreditBalance.java"
      to: "platform_credit_balance table"
      via: "@Table(name = \"platform_credit_balance\", schema = \"main\")"
      pattern: "schema.*main"
    - from: "PlatformCreditLedgerEntry.java"
      to: "PlatformLedgerEntryType"
      via: "@Enumerated(EnumType.STRING)"
      pattern: "PlatformLedgerEntryType"
    - from: "PlatformCreditBalanceRepository"
      to: "PlatformCreditBalance"
      via: "findForUpdate() SELECT FOR UPDATE"
      pattern: "PESSIMISTIC_WRITE"
---

<objective>
Create the database foundation for the platform credit account: Flyway migration V11 adding two tables and seeding the singleton balance row, plus the four JPA repo-layer files that map to those tables.

Purpose: Every subsequent plan in Phase 19 depends on these tables and entities existing. Plan 02 builds the service on top of these repositories. Plan 03 wires the REST layer. Nothing can proceed without the schema and JPA layer.
Output: V11 migration file, two entities, two repositories — all compilable with no business logic yet.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-platform-credit-account/19-RESEARCH.md

# Existing patterns to replicate exactly
@src/main/java/com/softropic/sendam/gateway/billing/repo/ClientCreditBalance.java
@src/main/java/com/softropic/sendam/gateway/billing/repo/ClientCreditBalanceRepository.java
@src/main/java/com/softropic/sendam/gateway/billing/repo/CreditLedgerEntry.java
@src/main/java/com/softropic/sendam/gateway/billing/repo/CreditLedgerRepository.java
@src/main/resources/db/migration/V3__credit_ledger.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flyway migration V11 — tables and singleton row</name>
  <files>src/main/resources/db/migration/V11__platform_credit_account.sql</files>
  <action>
Create V11__platform_credit_account.sql in src/main/resources/db/migration/.

The file must:
1. Create table main.platform_credit_balance with columns: id BIGINT PRIMARY KEY, balance BIGINT NOT NULL DEFAULT 0, status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE', created_by VARCHAR(50), created_date TIMESTAMP, last_modified_by VARCHAR(50), last_modified_date TIMESTAMP, request_id VARCHAR(100), session_id TEXT. Mirror column-for-column with the existing main.client_credit_balance table in V3 (omit client_id — platform balance has no FK).

2. Create table main.platform_credit_ledger_entry with columns: id BIGINT PRIMARY KEY, entry_type VARCHAR(30) NOT NULL, amount BIGINT NOT NULL, balance_after BIGINT NOT NULL, reference VARCHAR(200), status VARCHAR(20) NOT NULL DEFAULT 'ACTIVE', created_by VARCHAR(50), created_date TIMESTAMP, last_modified_by VARCHAR(50), last_modified_date TIMESTAMP, request_id VARCHAR(100), session_id TEXT. Mirror main.credit_ledger_entry from V3 (omit client_id).

3. Create index: CREATE INDEX idx_platform_ledger_type_date ON main.platform_credit_ledger_entry(entry_type, created_date DESC);

4. Insert singleton balance row: INSERT INTO main.platform_credit_balance (id, balance, status, created_date) VALUES (1, 0, 'ACTIVE', NOW());
   Use id=1. This is safe — TSID-generated IDs from io.hypersistence.utils encode a timestamp with high-bit values far above 1. The application never generates a new row in this table; it always updates the singleton.

Do NOT add any columns beyond those listed. Do NOT use sequences for id — raw BIGINT primary key is correct for the singleton and ledger entries (TSID is applied by JPA @Tsid, not by a DB sequence).
  </action>
  <verify>
The file exists at src/main/resources/db/migration/V11__platform_credit_account.sql and contains both CREATE TABLE statements, the index, and the INSERT. Application starts cleanly: ./mvnw spring-boot:run (or test run) shows Flyway applied V11 with no errors.
  </verify>
  <done>
Flyway migrates V11 on startup with no errors. Both tables exist in main schema. SELECT * FROM main.platform_credit_balance returns exactly one row with balance=0 and status=ACTIVE.
  </done>
</task>

<task type="auto">
  <name>Task 2: JPA entities and repositories for platform credit</name>
  <files>
    src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditBalance.java
    src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditBalanceRepository.java
    src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditLedgerEntry.java
    src/main/java/com/softropic/sendam/gateway/billing/repo/PlatformCreditLedgerRepository.java
  </files>
  <action>
Create four files in package com.softropic.sendam.gateway.billing.repo, mirroring the existing client credit files exactly.

PlatformCreditBalance.java:
- Annotations: @Entity, @Table(name = "platform_credit_balance", schema = "main"), @SuperBuilder, @NoArgsConstructor, @AllArgsConstructor, @Getter, @Setter
- Extends AbstractAuditingEntity (from com.softropic.sendam.common.persistence)
- Fields: long balance (column "balance", not null); EntityStatus status with @Builder.Default = EntityStatus.ACTIVE
- No clientId field — this is the singleton, no FK needed
- Exact same import set as ClientCreditBalance.java

PlatformCreditBalanceRepository.java:
- Extends JpaRepository<PlatformCreditBalance, Long>
- Two query methods:
  1. findForUpdate(): @Lock(PESSIMISTIC_WRITE) + @Query("SELECT b FROM PlatformCreditBalance b") + @QueryHints({@QueryHint(name = "jakarta.persistence.lock.timeout", value = "2000")}) — returns Optional<PlatformCreditBalance>
  2. findBalance(): @Query("SELECT b FROM PlatformCreditBalance b") — returns Optional<PlatformCreditBalance> (no lock, for reads)
- Exact same import set as ClientCreditBalanceRepository.java (LockModeType, QueryHint, etc.)

PlatformCreditLedgerEntry.java:
- Annotations: @Entity, @Table(name = "platform_credit_ledger_entry", schema = "main"), @SuperBuilder, @NoArgsConstructor, @AllArgsConstructor, @Getter, @Setter
- Extends AbstractAuditingEntity
- Fields:
  - @Enumerated(EnumType.STRING) @Column(name = "entry_type", nullable = false, length = 30) PlatformLedgerEntryType entryType
  - @Column(name = "amount", nullable = false) long amount (Javadoc: signed: positive = credit, negative = debit)
  - @Column(name = "balance_after", nullable = false) long balanceAfter
  - @Column(name = "reference", length = 200) String reference
  - @Builder.Default EntityStatus status = EntityStatus.ACTIVE
- Import PlatformLedgerEntryType from com.softropic.sendam.gateway.billing.contract.PlatformLedgerEntryType (this class is created in Plan 02 — the import will compile once Plan 02 is complete; for now the entity will have a compile error on PlatformLedgerEntryType until Plan 02 adds that class)
- Actually: to avoid compile errors, create a stub PlatformLedgerEntryType enum NOW (in this task) alongside the entity, OR create the entity after Plan 02. Simplest: create PlatformLedgerEntryType.java as part of this task in the contract package with all three values: NEXAH_PURCHASE, TOPUP_DEBIT, SHORTFALL_ABSORPTION. Plan 02 will NOT recreate it — it already exists.

PlatformLedgerEntryType.java (bonus file, same task):
- Package: com.softropic.sendam.gateway.billing.contract
- File: src/main/java/com/softropic/sendam/gateway/billing/contract/PlatformLedgerEntryType.java
- Content: public enum PlatformLedgerEntryType { NEXAH_PURCHASE, TOPUP_DEBIT, SHORTFALL_ABSORPTION }
- Three values required by PLAT-03. SHORTFALL_ABSORPTION is used in later phases but the enum value must exist now so the VARCHAR(30) column constraint accommodates it.

PlatformCreditLedgerRepository.java:
- Extends JpaRepository<PlatformCreditLedgerEntry, Long>
- One paginated query method with optional entry_type filter:
  @Query("SELECT e FROM PlatformCreditLedgerEntry e WHERE (:type IS NULL OR e.entryType = :type) ORDER BY e.createdDate DESC")
  Page<PlatformCreditLedgerEntry> findByOptionalType(@Param("type") PlatformLedgerEntryType type, Pageable pageable);
- Imports: Page, Pageable, PageRequest from Spring Data; JpaRepository; @Query, @Param
  </action>
  <verify>
./mvnw compile runs with no errors. All four repo files (plus PlatformLedgerEntryType) exist in their correct packages. No red imports.
  </verify>
  <done>
All five files compile cleanly. PlatformLedgerEntryType has three values. PlatformCreditBalanceRepository.findForUpdate() uses PESSIMISTIC_WRITE with 2000ms timeout. PlatformCreditLedgerRepository.findByOptionalType() accepts null type (returns all) or a specific type (filters).
  </done>
</task>

</tasks>

<verification>
1. ./mvnw compile exits 0 — no compilation errors
2. Application starts and Flyway log shows "Successfully applied migration V11__platform_credit_account.sql"
3. psql: SELECT count(*) FROM main.platform_credit_balance; → 1
4. psql: SELECT balance, status FROM main.platform_credit_balance; → balance=0, status=ACTIVE
</verification>

<success_criteria>
- V11 migration applied cleanly on startup
- Singleton balance row exists with balance=0
- PlatformCreditBalance, PlatformCreditLedgerEntry, both repos, PlatformLedgerEntryType all compile
- PlatformCreditBalanceRepository.findForUpdate() uses SELECT FOR UPDATE with 2000ms timeout
- PlatformLedgerEntryType has NEXAH_PURCHASE, TOPUP_DEBIT, SHORTFALL_ABSORPTION
</success_criteria>

<output>
After completion, create .planning/phases/19-platform-credit-account/19-01-SUMMARY.md
</output>
